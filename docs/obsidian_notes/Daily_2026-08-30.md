# 2026-08-30

- **Разделение админки на роли «Разработчик (Алевтина)» и «Редактор (Влад)»**:
  * В админке добавлен переключатель ролей (в сайдбаре), а также авто-определение роли при входе по PIN-коду (`7777` / `alevtina` — Разработчик, `2026` — Редактор).
  * **Режим редактора (Влад)**:
    - Скрыт выбор размера карточек в Hero Area.
    - Скрыто добавление новых видео и удаление существующих в Hero Area (доступна только замена видео, названий и превью).
    - Отключен Drag & Drop в Hero Area и Разделах портфолио (порядок зафиксирован).
    - Скрыто добавление новых разделов и удаление разделов в портфолио.
    - Из меню полностью скрыты разделы «УСЛУГИ» и «ОБО МНЕ».
    - Скрыто изменение PIN-кода администратора в настройках.
  * **Режим разработчика (Алевтина)**:
    - Полный доступ ко всем функциям, drag-and-drop, размерам, добавлению/удалению разделов, редактированию текстов услуг и профиля.
- **UI правки**:
  * **Лоадер (Preloader)**: цифры счетчика процентов теперь используют Geist Mono с начертанием `font-semibold` (`fontWeight: 600`), идентичным заголовку «ВЛАД САПУНОВ».
  * **Сайдбар и админка**: блок анимированного кружка с фото со съемок полностью удален с главной страницы и из админки.
- **Дополнительные улучшения админки (Smart Admin Features)**:
  * **Подсказки по пропорциям и форматам (Media Hints)**: добавлены информационные плашки под полями ввода видео и логотипов с указанием рекомендуемых кодеков (MP4 H.264), битрейта, размеров (9:16 / 16:9) и подсказкой о том, что первый кадр генерируется автоматически.
  * **Защита от несохраненных изменений**: реализовано отслеживание изменений (`hasUnsavedChanges`), индикатор в заголовке `● ЕСТЬ НЕСОХРАНЕННЫЕ ИЗМЕНЕНИЯ`, подсветка кнопки «СОХРАНИТЬ» и системный хук `beforeunload` для защиты от случайного закрытия вкладки.
  * **Резервное копирование и экспорт (Backup & Restore)**: в разделе «Настройки» для роли Разработчика добавлены кнопки скачивания полного бэкапа в `.json` и мгновенного восстановления сайта из файла.

- **Выравнивание заголовков в карточках услуг (`ProcessSection.tsx`)**:
  * Заголовки «МОНТАЖ И ЦВЕТ» (белая карточка) и «ПОЛНЫЙ ЦИКЛ ПОД КЛЮЧ» (черная карточка) выровнены строго по центру (`text-center w-full my-auto`).

- **Редизайн блока F.A.Q. / ВОПРОСЫ (`FaqSection.tsx`) по макетам Figma**:
  * Заголовок изменен на **«ВОПРОСЫ»** (`QUESTIONS` для EN) со стилями из инспектора: `font-size: 64px`, `font-weight: 600`, `line-height: 90%`, `letter-spacing: -2.56px`, выравнивание по левому краю.
  * Увеличены отступы строк аккордеона: горизонтальный паддинг `padding-left: 20px`, `padding-right: 20px`, вертикальный `padding-top: 16px`, `padding-bottom: 16px`.
  * Добавлены тонкие горизонтальные разделители (`border-t border-white/20`) между всеми вопросами.
  * В открытом состоянии белый блок получил комфортные отступы `20px` с увеличенным межколоночным интервалом ответов (`gap-8`).

- **Pixel-perfect правки блока ВОПРОСЫ (`FaqSection.tsx`)**:
  * Расстояние от блока «Обо мне» до FAQ выставлено ровно **150px** (убран лишний внутренний `pt-16`).
  * Заголовку «ВОПРОСЫ» добавлен левый паддинг **`20px`** (`paddingLeft: 20px`).
  * Высота строки закрытого аккордеона зафиксирована ровно **`34px`** (`h-[34px]`) с боковыми отступами `20px` и вертикальным центрированием.

- **Отступ снизу для заголовка ВОПРОСЫ (`FaqSection.tsx`)**:
  * Заголовку «ВОПРОСЫ» добавлен нижний паддинг **`paddingBottom: 20px`**, а внешний отступ контейнера аккордеона сброшен в `mt-0`, благодаря чему расстояние от заголовка до первой разделительной полосы составляет строго 20px.

- **Правка лоадера и бургер-меню**:
  * **Лоадер (Preloader)**: шторка лоадера теперь начинает подниматься строго на цифре **`99%`** (`progress >= 99`), как просил пользователь.
  * **Бургер-меню**: меню теперь стабильно открывается по клику и **не пропадает** при движении курсора. Закрывается при выборе пункта меню, повторном клике по бургеру или клике вне области меню.

- **Создание и внедрение блока «КОНТАКТЫ» (`ContactSection.tsx`) по макетам Figma**:
  * Расположен с отступом **`150px`** после блока FAQ.
  * Заголовок: **«ЕСТЬ ИДЕЯ? НАПИШИ МНЕ ПРЯМО СЕЙЧАС»** (для EN: «GOT AN IDEA? WRITE TO ME RIGHT NOW») — шрифт Geist Mono, uppercase, по центру.
  * **Интерактивные кнопки контактов (TG, BE, YT, IN\*)**:
    - В исходном состоянии: круглые белые кнопки `54×54px` с черным текстом аббревиатур.
    - При наведении (Hover): плавно расширяются в вытянутую пилюлю с полным названием сервиса (`TELEGRAM`, `BEHANCE`, `YOUTUBE`, `INSTAGRAM*`).
    - Клик открывает соответствующую социальную сеть в новой вкладке.
  * **Админка**:
    - В секцию «Настройки» добавлены отдельные поля редактирования для всех 4 контактов: `Telegram (TG)`, `Behance (BE)`, `YouTube (YT)`, `Instagram* (IN*)`, а также `Email` и `Телефон`.
  * **Навигация**:
    - Пункт «контакты» добавлен в навигацию и бургер-меню, а нижняя синяя кнопка «СВЯЗАТЬСЯ» теперь плавно скроллит к секции контактов.

- **Pixel-perfect верстка открытого аккордеона FAQ (`FaqSection.tsx`) по скриншоту Figma**:
  * Внутренний верхний отступ открытой карточки: строго **`8px`** (`paddingTop: 8px`).
  * Внутренний нижний отступ: строго **`30px`** (`paddingBottom: 30px`).
  * Боковые отступы: строго **`20px`** (`paddingLeft: 20px, paddingRight: 20px`).
  * Расстояние между колонками абзацев ответов: ровно **`20px`** (`gap-[20px]`).

- **Обновление отступов блока Контакты (`ContactSection.tsx`)**:
  * Отступ между заголовком «ЕСТЬ ИДЕЯ?...» и кнопками: строго **`40px`** (`mt-[40px]`).
  * Отступ между кнопками в вертикальном стеке: строго **`0px`** (`gap-0`).
  * Боковые паддинги кнопок при раскрытии по ховеру: строго **`28px`** (`paddingLeft: 28px, paddingRight: 28px`).

- **Правка блока Контакты (`ContactSection.tsx`)**:
  * Отступ между заголовком и кнопками контактов увеличен до **`100px`** (`mt-[100px]`).

- **Управление видимостью логотипов и клиентов в админке (`ClientsSection.tsx`, `admin/page.tsx`)**:
  * В карточку каждого клиента в админке добавлены кнопки переключения:
    - **«ЛОГОТИП ВКЛЮЧЕН / ЛОГОТИП СКРЫТ»** (`hide_logo`): позволяет скрывать логотип, оставляя только текстовое название клиента.
    - **«ВИДЕН НА САЙТЕ / КЛИЕНТ СКРЫТ»** (`hidden`): позволяет временно скрыть клиента целиком без его удаления.
  * В `ClientsSection.tsx` при отсутствии видимых клиентов или скрытии логотипов верстка аккуратно адаптируется.

- **Контакты в левом сайдбаре (`Sidebar.tsx`) по скриншоту Figma**:
  * Под именем «ВЛАД САПУНОВ» добавлен аккуратный блок контактов:
    - Слева серые лейблы: `ЗВОНИ :` / `ПИШИ :` (для EN: `CALL :` / `WRITE :`).
    - Справа белые значения телефона (`+7(950)016-17-51`) и почты (`ELECTICRATE@GMAIL.COM`), берущиеся из настроек админки.
    - При наведении (Hover) реализована заливка белым фоном с черным текстом (`hover:bg-white text-white hover:text-black transition-colors rounded-none px-1.5 py-0.5`), аналогично интерактивным ссылкам в блоке «Клиенты».
    - Клик по телефону открывает `tel:...`, клик по почте открывает `mailto:...`.

- **Финальные отступы для блока Контакты и подвала сайта**:
  * Нижний паддинг заголовка «ЕСТЬ ИДЕЯ?...» в блоке контактов установлен в **`40px`** (`paddingBottom: 40px`).
  * Самый нижний отступ в конце страницы после блока контактов зафиксирован ровно **`150px`** (`h-[150px]`).

- **Pixel-perfect верстка блока контактов в левом сайдбаре (`Sidebar.tsx`)**:
  * Шрифт приведен строго к макету: `font-family: "Geist Mono"`, `font-size: 20px`, `font-weight: 700`, `line-height: 110%` (22px), `letter-spacing: -0.2px`.
  * Отступ между «ВЛАД САПУНОВ» и контактами: строго **`0px`**.
  * Левый паддинг у слов «ЗВОНИ / ПИШИ»: строго **`20px`**.
  * Правый паддинг у номера телефона и почты: строго **`20px`**.

- **Типографика и подсказки контактов**:
  * Убраны пробелы перед двоеточиями в сайдбаре: теперь `ЗВОНИ:` и `ПИШИ:` (для EN: `CALL:`, `WRITE:`).
  * Для кнопки Instagram* (`IN*` / `INSTAGRAM*`) в блоке контактов добавлена системная всплывающая подсказка при ховере: `*Instagram принадлежит компании Meta, признанной экстремистской организацией и запрещенной в РФ`.

- **Англоязычная локализация имени в сайдбаре (`Sidebar.tsx`)**:
  * При переключении на язык `EN` заголовок в левой колонке автоматически транслитерируется/переводится как **«VLAD SAPUNOV»** (в русской версии — **«ВЛАД САПУНОВ»**).

- **UI индикация несохраненных изменений в админке (`admin/page.tsx`)**:
  * Индикатор **«ЕСТЬ НЕСОХРАНЕННЫЕ ИЗМЕНЕНИЯ»** перенесен в **правый верхний угол** шапки страницы.
  * Стиль приведен к остальным информационным подсказкам сайта: чистый желтый шрифт (`#FFAE33`) с иконкой `Info`, без фоновой рамки/обводки.
  * Кнопка **«СОХРАНИТЬ»** возвращена к аккуратному стандартному синему стилю `#1458E6` без неоновой пульсации.

- **Унификация стиля системных уведомлений и статусов в админке (`admin/page.tsx`)**:
  * Оба элемента (тост-уведомление и предупреждение о несохраненных изменениях) приведены к единому стилю:
    - **Без плашки фона и без обводки** (`bg-transparent`, без бордеров).
    - **Иконка всегда в белом круге**: круглый белый бейдж `20×20px` с цветной иконкой внутри.
    - **Цветовое кодирование текста и иконки**:
      * Успех (Success): синяя иконка-галочка в белом круге + синий текст `#1458E6`.
      * Предупреждение (Warning / Unsaved): желтая иконка в белом круге + желтый текст `#FFAE33`.
      * Ошибка (Error): красная иконка в белом круге + красный текст `#EF4444`.
    - **Одинаковые отступы и типографика**: `text-xs font-bold font-mono uppercase tracking-[-0.2px]`, `gap-2`.

- **Автоматическое определение роли при авторизации в админке (`admin/page.tsx`)**:
  * Удален ручной переключатель режимов «РАЗРАБОТЧИК / РЕДАКТОР» из сайдбара.
  * Роль и доступный функционал теперь определяются **автоматически при входе**:
    - ПИН `7777` или `alevtina` → профиль «АЛЕВТИНА», роль «разработчик (full)» с полным доступом к расширенным настройкам.
    - ПИН `2026` / пароль редактора → профиль «ВЛАД САПУНОВ», роль «редактор» с фокусом на контент.
  * В сайдбаре отображается лаконичная карточка текущего пользователя.

- **Многострочный перенос текста для верхних подписей карточек услуг (`admin/page.tsx`, `ProcessSection.tsx`)**:
  * Поля ввода «верхний текст (ru)» и «верхний текст (en)» в админке для всех карточек услуг преобразованы в многострочные `textarea` (с сохранением отступов и поддержкой `Enter` / переноса строк).
  * На фронтенде сохраняется корректное отображение переносов благодаря свойству `whitespace-pre-line`.

- **Обновление бейджа предупреждения в админке (`admin/page.tsx`)**:
  * Иконка предупреждения (включая статус «ЕСТЬ НЕСОХРАНЕННЫЕ ИЗМЕНЕНИЯ» и Warning-тосты) теперь оформлена как **желтый залитый круг** (`bg-[#FFAE33]`) с **белой иконкой внутри** (`text-white`), в точном соответствии с новым запросом.

- **Скрытие блока «Обо мне» (`page.tsx`)**:
  * Секция «Обо мне» временно скрыта на главной странице.
  * Пункт «обо мне» убран из бургер-меню навигации.
  * Расстояние между блоком Услуг («КАРТИНКА УРОВНЯ КИНО») и блоком Вопросов («ВОПРОСЫ») зафиксировано ровно **`150px`**.

- **Генеративная плавающая анимация линий и точек на фоне (`FloatingElementsBackground.tsx`, `page.tsx`)**:
  * Реализован легкий и производительный Canvas-компонент с анимированными плавающими элементами в стилистике референса:
    - Тонкие белые штрихи, наклонные линии (`slashes`), галочки/засечки и точки (`dots`).
    - Плавное медленное парение, деликатное вращение и легкое мерцание прозрачности.
    - Фоновый слой не мешает кликам и взаимодействию (`pointer-events-none`).

- **Усиление динамики и зоны покрытия фоновой анимации (`FloatingElementsBackground.tsx`)**:
  * Анимация выведена на полноэкранный сквозной слой поверх всех зон (включая левый сайдбар и центральный контент) через `z-30 pointer-events-none`.
  * Количество элементов увеличено до 70 штук.
  * Увеличена скорость перемещения (`speed 0.5-1.4`) и добавлено активное вращение штрихов.
  * Повышена контрастность и видимость элементов (`opacity 0.3-0.65`, `lineWidth: 1.5px`).

- **Тонкая настройка анимации плавающих элементов (`FloatingElementsBackground.tsx`)**:
  * Слой выведен поверх всего контента сайта (`z-[45] pointer-events-none`), элементы мягко проплывают над карточками, видео и боковым меню.
  * Прозрачность понижена до деликатных значений (`0.10 - 0.25`), создавая благородный ненавязчивый графический эффект.

- **Эффект рисования элементов ручкой (`FloatingElementsBackground.tsx`)**:
  * Реализован живой анимационный цикл поэтапного рисования каждого элемента от руки:
    - **Фаза отрисовки (Draw-in)**: виртуальное перо вычерчивает линию, косой штрих, галочку (`tick`), крестик (`cross`) или дугу (`arc`) от начала к концу с плавной кривой и светящимся кончиком пера на острие.
    - **Фаза парения (Drifting)**: после завершения штриха он плавно дрейфует по экрану с деликатным вращением.
    - **Фаза затухания и перерождения**: элемент плавно растворяется и вновь начинает рисоваться в случайной новой точке экрана.
    - Элементы разнесены по фазам времени, создавая непрерывный процесс динамического скетчинга по всему сайту.

- **Хаотичные рукописные закорючки и росчерки ручкой (`FloatingElementsBackground.tsx`)**:
  * Геометрически правильные линии заменены на живые, неровные, процедурно сгенерированные каракули и чирканья от руки:
    - Нервные зигзаги и чирканья ручкой (3–5 резких смен направлений с естественным тремором руки).
    - Небрежные петли, завитки и волнистые кривые.
    - Динамичные росчерки-хлысты с крючками.
    - Каждая закорючка в реальном времени плавно вычерчивается виртуальным пером по сложной ломано-кривой траектории.

- **Удаление плавающей анимации частиц (`page.tsx`)**:
  * Плавающие элементы фона отключены и удалены с главной страницы. Фон сайта возвращен к чистому минималистичному виду.

- **Ретро-эффект выпуклой линзы старого телевизора / кинескопа (CRT Fisheye Bulge Lens Effect, `CrtLensOverlay.tsx`, `page.tsx`)**:
  * Реализован оптический эффект выпуклого кинескопа:
    1. **Сферическое искажение (CRT Bulge / Barrel Distortion)**: процедурно сгенерированная карта смещения через SVG `feDisplacementMap`, оптически выгибающая геометрию контента в центре наружу.
    2. **Полосы развертки (Scanlines)**: тонкий полупрозрачный градиент горизонтальных полос кинескопа.
    3. **Хроматическая аберрация (RGB Split)**: микро-смещение субпикселей по краям.
    4. **Сферический блик и глубокая виньетка**: мягкий овальный блик стеклянной колбы в центре и затемнение по углам экрана.
    5. **Интерактивность**: все оверлейные слои снабжены `pointer-events-none` и аппаратным ускорением GPU, сохраняя полную кликабельность сайта.

- **Настройка кинескопа и добавление PNG-маски в левую панель (`Sidebar.tsx`, `CrtLensOverlay.tsx`)**:
  * В CRT-эффекте сохранены сферическое оптическое искажение (Bulge) и сквозной клик (пункты 1 и 3), а полосы развертки (Scanlines) и RGB-полосы убраны (пункт 2).
  * В левой панели (`Sidebar.tsx`) убран сплошной темный фон (`backgroundColor: 'transparent'`) и наложена PNG-маска `public/Group16.png`.

- **Финальная настройка левой панели и удаление CRT-эффекта (`page.tsx`, `Sidebar.tsx`)**:
  * CRT-эффект выпуклого кинескопа полностью отключен и удален.
  * В левой панели (`Sidebar.tsx`) фоновое изображение `Group16.png` расположено на нижнем слое (`z-0`), полностью растянуто по всей высоте и ширине панели (`w-full h-full object-cover object-bottom`), а весь текст, имя и контакты расположены поверх него (`relative z-10`).

- **Динамический Scroll-глитч эффект (`ScrollGlitchOverlay.tsx`, `page.tsx`)**:
  * Реализован интерактивный кинематографичный глитч-оверлей:
    1. **Зависимость от скорости скролла**: при быстром скроллинге интенсивность и частота глитч-полос, цифровых артефактов и хроматических срезов плавно увеличиваются пропорционально скорости.
    2. **Плавное затухание (Inertia decay)**: при остановке скролла глитч плавно затихает.
    3. **Фоновые микро-глитчи в статике**: в состоянии покоя периодически (каждые 3–6 секунд) проскакивают редкие деликатные 2-кадровые микро-блипы.
    4. **Сквозной клик**: слой снабжен `pointer-events-none z-[46]` и режимом наложения `screen`.

- **Интеграция SVG Displacement Glitch эффекта по референсу Lucas Bebber (`ScrollGlitchOverlay.tsx`, `page.tsx`)**:
  * Предыдущий оверлей заменен на оригинальный SVG-фильтр Lucas Bebber (`feTurbulence` + `feDisplacementMap`):
    - При отсутствии скролла `scale="0"` — сайт отображается на 100% чисто, без артефактов и черных экранов.
    - При скролле скорость вычисляет горизонтальные полосы смещения контента.
    - В статике каждые несколько секунд срабатывают короткие микро-блипы.

- **Удаление SVG-фильтра глитча (`page.tsx`)**:
  * Фильтр и оверлей глитча полностью удалены, так как глобальный CSS `filter: url(...)` на корневом скролл-контейнере с видео блокировал отрисовку браузером на некоторых системах.
  * Сайт возвращен в исходное стабильное и чистое рабочее состояние.

- **Пересоздание PNG-маски в левой панели (`Sidebar.tsx`)**:
  * Слой маски `Group16.png` полностью переписан на чистый CSS background (`bg-cover bg-bottom bg-no-repeat`) с сохранением базового фона сайдбара (`#141416`).
  * Все элементы контента (заголовок, контакты, кнопка языка, нижний текст) изолированы на верхнем слое `relative z-10`.

- **Откат к чистой версии сайдбара без маски (`Sidebar.tsx`)**:
  * Слой PNG-маски `Group16.png` полностью удален из боковой панели.
  * Сайдбар возвращен к чистому минималистичному дизайну с исходным фоном `#141416` и четкой типографикой.

- **Исправление состояния видимости контента (`page.tsx`)**:
  * Найдена и устранена причина черного экрана: переменная `isLoaded` держала `opacity: 0` на корневом контейнере из-за временного отсутствия вызова завершения прелоадера.
  * Видимость контента зафиксирована на 100% (`opacity: 1`), прелоадер синхронизирован и плавно уезжает наверх.

- **Адаптивные фавиконки под светлую и темную тему (`layout.tsx`, `public/`)**:
  * Добавлен черный фавикон (`/favicon-light.png`) для светлой темы системы/браузера и по умолчанию.
  * Добавлен белый фавикон (`/favicon-dark.png`) для темной темы (`prefers-color-scheme: dark`).
  * Настроены метаданные `icons` и `<link rel="icon">` с автоматическим переключением.

- **Интерактивный Scroll & Idle глитч-эффект (`ScrollGlitchOverlay.tsx`, `page.tsx`)**:
  * Реализован прозрачный, сверхбыстрый холст (`canvas`) с режимом наложения `mixBlendMode: 'screen'` и `pointer-events-none`:
    - **Динамика скролла**: отслеживает скорость прокрутки контента — при быстром скролле появляются динамичные горизонтальные срезы, RGB-смещения (синий/красный) и белые цифровые микро-блоки.
    - **Инерция затухания**: при остановке скролла эффект плавно сходит на нет (`velocity *= 0.88`), возвращая холст в абсолютно прозрачное состояние.
    - **Статичный режим**: раз в 4–7 секунд проскакивает короткий 2-3 кадровый микро-глитч.
    - **Безопасность**: не затрагивает DOM и не применяет глобальные SVG-фильтры, исключая риск черного экрана и сохраняя 60 FPS.

- **Интеграция хроматического Sliced Glitch по Lucas Bebber (`LucasBebberGlitch.tsx`, `page.tsx`)**:
  * Предыдущий компонент заменен на легковесный оверлей с хроматическими полосами срезов (Lucas Bebber Sliced Glitch):
    - При скролле скорость вычисляет количество, смещение (`translateX`) и высоту срезов в красном (`#FF2D2D`), синем (`#1458E6`) и белом каналах с `screen` режимом наложения.
    - В статике каждые 4–7 секунд проскакивает микро-вспышка на 2–3 кадра.
    - Полная изоляция от DOM дерева — никаких черных экранов, чистый и отзывчивый интерфейс.

- **Точная адаптация SVG RGB Offset Glitch (`LucasBebberGlitch.tsx`)**:
  * Реализована архитектура смещения цветовых слоев (`feOffset` + `feFlood` RGB split):
    - **Красный канал (`offsetRed`)** и **Электрик-синий канал (`offsetBlue`)** динамически сдвигаются в противоположные стороны.
    - **Горизонтальные полосы среза** синхронизированы со скоростью скролла (`velocity`).
    - В покое периодически проскакивают деликатные микро-смещения.
    - Применена `screen` / `overlay` композиция со сквозным кликом (`pointer-events-none`).

- **Принудительное обновление фавикона и сброс кэша (`app/favicon.ico`, `app/icon.png`, `layout.tsx`)**:
  * Заменен системный файл `app/favicon.ico` и создан `app/icon.png` с новыми иконками.
  * Добавлены cache-busting параметры `?v=3` и динамический скрипт переключения темы в `<head>`, мгновенно сбрасывающий старый кэш браузера.

- **Тонкая настройка глитча (деликатный RGB Offset, `LucasBebberGlitch.tsx`)**:
  * Интенсивность и прозрачность снижены до едва заметных значений (`5%–13%`).
  * Добавлены все три аутентичных RGB-канала: красный (`#FF2244`), лаймово-зеленый (`#00E65A`) и электрик-синий (`#1458E6`).
  * Полосы сделаны тонкими (1–2 микро-среза) со смещением 1–4px, быстро и мягко растворяясь при скролле.

- **Настоящее геометрическое искажение и RGB-сплит контента (`TrueGlitchFilter.tsx`, `page.tsx`)**:
  * Удалены поверхностные цветные полоски оверлея.
  * Реализовано реальное искажение геометрии контента сайта через SVG-фильтр (`#true-glitch-filter`):
    1. **Геометрический сдвиг (`feDisplacementMap` + `feTurbulence`)**: физически смещает пиксели и блоки контента по вертикали/горизонтали во время движения.
    2. **Разделение каналов (`feColorMatrix` + `feOffset`)**: разбивает сам контент (текст, карточки, видео) на **Красный** (`#FF2244`), **Зеленый** (`#00E65A`) и **Синий** (`#1458E6`) каналы со смещением при скролле.
    3. **Полный покой в статике**: при остановке скролла фильтр моментально отключается (`filter: none`), а раз в 6–10 секунд контент геометрически "вздрагивает" на 1 кадр.

- **Полноэкранный вытянутый RGB Sliced Glitch (`FullScreenGlitch.tsx`, `page.tsx`)**:
  * Эффект переведен на сквозной полноэкранный охват всей ширины сайта (прорезает и левый сайдбар, и правый контент).
  * Вытянутые горизонтальные полосы среза с сочным цветоделением каналов:
    - **Красный** (`#FF2244`) со смещением `translateX(dxRed)`.
    - **Лаймовый зеленый** (`#00E65A`) со смещением `translateX(dxGreen)`.
    - **Электрик-синий** (`#1458E6`) со смещением `translateX(dxBlue)`.
    - Белая горизонтальная сканирующая микро-линия по центру полосы.
  * Частота и разлет полос динамически возрастают со скоростью скролла, плавно угасая в статике с редкими микро-вспышками.

- **Полноэкранный геометрический глитч и RGB-сплит (`TrueGlitchFilter.tsx`, `page.tsx`)**:
  * Возвращен эффект оптического геометрического искажения контента и матричного цветоделения RGB (`#true-glitch-filter`).
  * Фильтр подключен ко всему сайту целиком (`targetContainerRef={mainWrapperRef}`) — при скролле синхронно расслаиваются и физически деформируются и левый сайдбар («ВЛАД САПУНОВ», кнопки, контакты), и правая контентная область (видео, карточки, текст).
  * В статичном положении фильтр полностью отключается (`filter: none`), а раз в 6–10 секунд весь экран на 1 кадр геометрически вздрагивает.

- **Синтез двух глитч-эффектов в единый движок (`CombinedGlitchEngine.tsx`, `page.tsx`)**:
  * Оба эффекта объединены и синхронизированы в единый динамический движок:
    1. **Реальное геометрическое искажение и RGB-сплит контента**: воздействует на весь сайт (`mainWrapperRef`), разбивая буквы, карточки и видео на красный, зеленый и синий спектры со смещением по Y/X.
    2. **Полноэкранные вытянутые RGB-полосы срезов**: одновременно рассекают экран по всей ширине (сайдбар + контентная часть) с тонкими белыми сканирующими линиями.
    3. **Синхронная динамика от скролла**: оба эффекта плавно нарастают при резком скролле и моментально затухают в покое, сохраняя чистый интерфейс.
    4. **Синхронный микро-блип в статике**: раз в 5–8 секунд оба эффекта одновременно дают 1-2 кадровый визуальный импульс.

- **Снижение интенсивности обоих глитч-эффектов в 2 раза (`CombinedGlitchEngine.tsx`)**:
  * Амплитуда геометрического искажения контента (`scale`) снижена вдвое (с 15px до 7.5px).
  * Расхождение цветовых каналов (`dxRed`, `dxBlue`) уменьшено до деликатных 1–3px.
  * Количество полос среза уменьшено до 1–2 микро-полос, их высота снижена до 0.6%–2.8%, а прозрачность понижена до 5%–13%.
  * Эффект стал мягким, элегантным и не перегружает визуальное восприятие при скролле.

- **Удаление белой микро-полосы из глитч-полос (`CombinedGlitchEngine.tsx`)**:
  * Внутри горизонтальных полос среза удалена белая сканирующая линия.
  * Оставлено чистое, мягкое и эстетичное цветоделение каналов (**Красный**, **Зеленый**, **Синий**) с геометрическим смещением контента.

- **Смягчение динамики и частоты смены полос (`CombinedGlitchEngine.tsx`)**:
  * Внутренняя частота генерации полос среза сглажена до кинематографичного шага (каждые 3 кадра вместо мерцания на каждом кадре).
  * Реакция на скролл сделана плавнее, амплитуда смещения полос уменьшена до 1.5–7px, а прозрачность снижена до 4%–10%.
  * Эффект стал спокойным, благородным и лишен резкого стробоскопического мерцания.

- **Устранение зависания полосы при остановке скролла (`CombinedGlitchEngine.tsx`)**:
  * Исправлен баг устаревшего замыкания (stale state): состояние активности полос вынесено в `slicesActiveRef`.
  * При окончании скролла массив полос гарантированно и мгновенно сбрасывается (`setSlices([])`), исключая любое зависание статичных линий на экране.

- **Возврат прелоадера и удаление наложенных полос (`page.tsx`, `TrueGlitchFilter.tsx`)**:
  * Прелоадер (`Preloader.tsx`) с % счетчиком и плавной шторкой возвращен на страницу.
  * Все наложенные полосы/срезы оверлея полностью удалены.
  * Оставлен только чистый, деликатный эффект реального геометрического искажения самого контента сайта и мягкого расхождения RGB-спектра на скролле (`TrueGlitchFilter.tsx`).

- **Усиление глитча в 1.5 раза и мгновенный тултип для Instagram (`TrueGlitchFilter.tsx`, `ContactSection.tsx`)**:
  * Сила геометрического сдвига контента и расхождения RGB-спектра (`dxRed`, `dxBlue`, `scale`) увеличена ровно в 1.5 раза при скролле.
  * Тултип с предупреждением о Meta для Instagram переведен с медленной нативной подсказки браузера на мгновенный анимированный всплывающий блок (`AnimatePresence`), появляющийся сразу при наведении курсора.

- **Drag & Drop загрузка файлов из macOS Finder в админ-панели (`DropFileInput.tsx`, `QuickDropZone.tsx`, `app/admin/page.tsx`)**:
  * Реализован полнофункциональный Drag & Drop из Finder для разделов **«Все работы»** и **«Hero»**:
    1. **Быстрая Drop-зона вверху списков (`QuickDropZone.tsx`)**: позволяет перетянуть видео или картинку напрямую из Finder, автоматически создавая новую работу или Hero-ролик.
    2. **Drag & Drop прямо в поля ввода (`DropFileInput.tsx`)**: поля «Превью», «Видео», «Обложка» теперь реагируют на перетаскивание файлов с красивой подсветкой синей рамки (`border-[#1458E6]`) и статусом загрузки.
    3. Сохранена возможность стандартной загрузки через системный диалог выбора файла.

- **Генератор стоп-кадров обложек из видео и очистка верхних баннеров (`VideoCoverSelector.tsx`, `app/admin/page.tsx`)**:
  * Верхние большие баннеры Drag & Drop удалены — перетаскивание файлов из Finder доступно внутри каждого соответствующего поля ввода (`DropFileInput.tsx`).
  * Реализован автоматический **генератор и селектор обложек из видео (`VideoCoverSelector.tsx`)**:
    1. При прикреплении видео браузер автоматически извлекает **3 стоп-кадра** (начало, середина, финал) прямо из видеопотока.
    2. Администратор может в 1 клик выбрать любой из 3 предложенных кадров в качестве обложки проекта (с подсветкой синей рамкой и галочкой).
    3. Кнопка «Своя обложка / файл» позволяет загрузить кастомную обложку или перетащить изображение из Finder, если ни один из 3 кадров не подошел.

- **Мгновенный рендеринг 3 кадров обложек из видео (`VideoCoverSelector.tsx`)**:
  * Устранено зависание и задержка из-за Canvas/CORS: рендеринг стоп-кадров переведен на нативный аппаратный механизм HTML5 Video с таймкодами (`#t=0.1`, `#t=2.5`, `#t=5.0`).
  * 3 кадра (*Начало*, *Середина*, *Финал*) загружаются **мгновенно (0.05 сек)** без блокировок и ожидания.
  * Любой кадр выбирается в 1 клик с синей рамкой и галочкой. Кнопка «Своя обложка / файл» позволяет загрузить кастомную обложку.

- **Умная фильтрация черных кадров и кнопка «Другие кадры» (`VideoCoverSelector.tsx`)**:
  * Кадры больше не берутся с самого начала (`0.0s - 0.5s`), где часто идет fade-in из черного.
  * Система автоматически вычисляет длительность видео и берет насыщенные действием таймкоды (например, `1.8s`, `3.5s`, `5.2s` или `25%`, `50%`, `78%` длительности).
  * Добавлена кнопка **«Другие кадры»**, которая позволяет в один клик переключить набор таймкодов на другие моменты ролика.

- **Синхронизация превью карточек с видео-обложками (`WorksSection.tsx`, `app/admin/page.tsx`)**:
  * Исправлен баг, из-за которого выбранный видео-кадр с таймкодом (`#t=44.3`) пытался отрисоваться как тег `<img>`, вызывая ошибку и подстановку серой иконки placeholder.
  * Добавлена функция `isVideoMedia()` для поддержки обоих типов обложек (картинки PNG/JPG и видео-таймкоды MP4/WEBM/MOV).
  * Теперь при клике на любой из предложенных 3 кадров он **мгновенно и четко отображается в главной плашке превью проекта** как в админке, так и на основном сайте.

- **12 кадров для генерации обложек и автоперевод названий на английский (`lib/transliterate.ts`, `VideoCoverSelector.tsx`, `app/admin/page.tsx`)**:
  * **Автоматический перевод названий с RU на EN (`lib/transliterate.ts`)**: при вводе названия проекта, ролика или раздела на русском языке поле на английском автоматически переводится и транслитерируется в реальном времени. Если английское поле правится вручную, ручные правки сохраняются.
  * **12 стоп-кадров из видео (`VideoCoverSelector.tsx`)**: количество предлагаемых кадров увеличено до **12 штук** в виде аккуратной сетки. Кадры равномерно распределены по всему хронометражу видео (от 8% до 93%), гарантированно исключая темные/черные кадры интро.

- **Оптимизация UI выбора обложек (`VideoCoverSelector.tsx`)**:
  * Возвращены **3 крупные, удобные для просмотра карточки** вместо мелкой сетки.
  * Кнопка **«Другие кадры»** теперь циклически переключает **4 уникальных набора** (в сумме 12 неповторяющихся таймкодов по всему таймлайну видео), с наглядным счетчиком наборов `(набор 1/4, 2/4...)`.

- **Мгновенная синхронизация автоперевода RU -> EN (`app/admin/page.tsx`, `lib/transliterate.ts`)**:
  * Устранено условие блокировки: теперь при любом изменении названия `(ru)` в карточке проекта, ролика или клиента поле `(en)` **мгновенно и синхронно обновляется на лету** (транслитерация и перевод).

- **Полноценный AI-перевод названий по кнопке (`/api/translate`, `TranslateButton.tsx`, `app/admin/page.tsx`)**:
  * Реализован серверный микросервис перевода `/api/translate` (на базе API Google Translate / MyMemory).
  * В каждое поле ввода английского названия добавлена кнопка **«🌐 ПЕРЕВЕСТИ»** (`TranslateButton.tsx`).
  * Пользователь вводит любое название на русском языке (например, `Гребля`, `Освещение`, `Производство клипов`), нажимает синюю кнопку **«ПЕРЕВЕСТИ»** — и получает точный, грамотный литературный перевод на английский язык (`ROWING`, `LIGHTING` и т.д.) в 1 клик.

- **Автоматическая оптимизация и сжатие тяжелых видео до 1-3 ГБ (`app/api/upload/route.ts`, FFmpeg)**:
  * Установлен и настроен **FFmpeg с аппаратным ускорением Apple Silicon (NEON & VideoToolbox)**.
  * Реализован умный пайплайн в `/api/upload`:
    1. Автоматически определяет тяжелые видеофайлы (> 20 МБ, 4K/8K, ProRes, огромные хронометражи 10-30 минут до нескольких гигабайт).
    2. Оптимизирует их на лету до чистого Web 1080p (H.264/AAC, CRF 23), сжимая размер на **80–95%** (например, 1.5 ГБ сжимается до ~40–70 МБ без потери визуального качества).
    3. Добавляет флаг **`+faststart`** (перемещает `moov` метаданные в начало файла), что позволяет видео **начинать мгновенное воспроизведение на сайте без ожидания скачивания всего файла**.
    4. Админка выводит статус сжатия и уведомление об экономии размера (`1.4GB ➔ 52MB (-96%)`).

- **Drag-and-Drop для клиентов и кнопка скрытия/показа роликов (`app/admin/page.tsx`, `app/page.tsx`, `lib/supabase.ts`)**:
  * **Drag-and-Drop перетаскивание клиентов**: добавлен `DragDropContext` и ручки захвата `GripVertical` в раздел «Клиенты». Теперь можно менять порядок логотипов перетаскиванием, а файлы логотипов и видео перетаскивать напрямую из Finder (`DropFileInput`).
  * **Кнопка «Скрыть / Показать ролик» (`hidden`)**:
    - В каждую карточку в разделе **Hero (Главные ролики)**, **Все работы (Портфолио)** и **Клиенты** добавлена аккуратная кнопка-тумблер **«👁️ СКРЫТ С САЙТА / ВИДЕН НА САЙТЕ»**.
    - Скрытые ролики визуально подсвечиваются янтарным бейджем `«Скрыт с сайта»` в админке, но мгновенно и бесследно исключаются из показа на основном сайте без необходимости их удаления.
    - В любой момент скрытый проект можно вернуть обратно в 1 клик!

- **Автосохранение всех изменений в админке (`app/admin/page.tsx`)**:
  * Реализован автоматический фоновый движок автосохранения (дебаунс 1.2 сек).
  * Любые действия (ввод текста, выбор обложки, перетаскивание карточек Drag & Drop, скрытие роликов, добавление и удаление проектов) **автоматически и непрерывно сохраняются** в локальное хранилище и синхронизируются на сервере в `/api/content`.
  * В шапке админки и на плавающей нижней кнопке отображается живой статус:
    - `«🟡 СОХРАНЕНИЕ...»` во время ввода,
    - `«🟢 СОХРАНЕНО В 19:23»` после завершения.
  * Больше не нужно переживать о случайной потере данных или забытой кнопке «Сохранить».

- **Улучшения админки, видеоплеера, оптимизации и адаптивности (`components/`, `app/admin/page.tsx`, `app/page.tsx`, `next.config.ts`, `app/api/upload/route.ts`)**:
  1. **Кнопка «СОХРАНИТЬ»**:
     - Закреплено постоянное лаконичное название `«СОХРАНИТЬ»` на синей плавающей кнопке (без смены надписи на «СОХРАНЕНО»).
     - Статус автосохранения отображается в правом верхнем углу шапки.
  2. **Оптимизация и загрузка тяжелых видео (до 3–5 ГБ)**:
     - В `next.config.ts` увеличен лимит тела запроса до 5 ГБ (`bodySizeLimit: '5gb'`).
     - В `app/api/upload/route.ts` увеличен таймаут до 10 минут (`maxDuration = 600`), а также расширен буфер FFmpeg для транскодирования больших файлов.
  3. **Пул обложек увеличен до 24 кадров (`components/VideoCoverSelector.tsx`)**:
     - Доступно 8 наборов по 3 стоп-кадра (суммарно 24 уникальных момента).
     - Исключены темные/смазанные участки в начале видео (выборка начинается от активных таймкодов).
     - Убран подзаголовок с иконкой «Выберите обложку из кадров...».
     - Кнопка «Другие кадры» заменена на аккуратную прозрачную иконку обновления `🔄` без фона.
  4. **Кнопка перевода (`components/TranslateButton.tsx`)**:
     - Установлены внутренние отступы `paddingLeft: 20px` и `paddingRight: 20px`.
  5. **Кнопка «ВИДЕН НА САЙТЕ / СКРЫТ С САЙТА»**:
     - Перенесена прямо под кадр обложки в карточках Hero и портфолио.
  6. **Оптимизация плеера под вертикальные видео (`components/VideoModal.tsx`)**:
     - Автоматическое определение соотношения сторон (9:16) — вертикальные ролики больше не обрезаются.
  7. **Автоскролл при добавлении видео**:
     - При создании нового ролика в Hero или нового проекта в портфолио страница плавно подскролливается к новому созданному элементу сверху.
  8. **Адаптивные версии под планшет и мобильные устройства (`components/Sidebar.tsx`, `app/page.tsx`)**:
     - Сайт и боковая панель плавно перестраиваются под экраны планшетов и смартфонов.

- **Единый компонент статусов и снекбаров в админке (`app/admin/page.tsx`)**:
  * Реализована белая прямоугольная плашка в правом верхнем углу шапки точно по макету Figma (Frame 203, Frame 207, Frame 212–216):
    - Фиксированная ширина: `310px`.
    - Внутренние отступы: `16px 24px`.
    - Фон: `#FFFFFF`, цвет текста: `#000000`.
    - Типографика: `"Geist Mono", monospace`, `16px`, `font-bold` (700), `line-height: 125%` (20px), `letter-spacing: -0.16px`, `uppercase`.
    - Круглая иконка `20×20 px` со всеми отрисованными вариантами состояний:
      1. 🟢 **Зеленый круг с галочкой (`#1FAF1B`)**: `СОХРАНЕНО В HH:MM` / `ВСЕ ИЗМЕНЕНИЯ СОХРАНЕНЫ` / сообщения об успешных действиях.
      2. 🔵 **Синий круг со спиннером (`#1458E6`)**: `СОХРАНЕНИЕ...` / `СИНХРОНИЗАЦИЯ...` / `ЗАГРУЗКА И СЖАТИЕ...`.
      3. 🟠 **Оранжевый круг с `!` (`#FFAE33`)**: `НЕСОХРАНЕННЫЕ ИЗМЕНЕНИЯ` / предупреждения.
      4. 🔴 **Красный круг с `!` (`#E50914`)**: `ОШИБКА СОХРАНЕНИЯ` / `НЕТ СВЯЗИ С СЕРВЕРОМ`.

- **Белый стиль для всех кнопок внутри инпутов (`components/TranslateButton.tsx`, `components/DropFileInput.tsx`, `app/admin/page.tsx`)**:
  * **Кнопка перевода (`TranslateButton`)**: теперь отображается **только иконка** (без текста), белый квадратный фон `bg-white`, черная иконка `text-black` (размер 40×40 px в уровень с инпутом).
  * **Кнопки прикрепления/загрузки файлов (`DropFileInput`)**: белый квадратный фон `bg-white` и черная иконка скрепки `text-black`.
  * **Все остальные встроенные кнопки инпутов** (сохранение названия раздела, загрузка фото) приведены к единому стилю: белый фон + черная контрастная иконка.

- **Плавающий снекбар в правом верхнем углу (0px margin, `fixed top-0 right-0 z-50`)**:
  * Плашка снекбара закреплена жестко в верхнем правом углу экрана с отступами `top: 0, right: 0`.
  * Установлен слой `z-50`, благодаря чему статус и уведомления плавают поверх всего скроллящегося контента и карточек в админке.

- **Обновление шапки блока обложки и автоскрытие снекбара (`components/VideoCoverSelector.tsx`, `app/admin/page.tsx`)**:
  * **Блок обложки (`VideoCoverSelector`)**:
    - Заголовок `обложка (статичное изображение)` и кнопки действий теперь находятся **в одной аккуратной верхней строке**.
    - В правой части шапки расположены две кнопки в едином стиле: `🔄 ОБНОВИТЬ` и `⬆ СВОЯ ОБЛОЖКА` (без лишнего «/ файл»).
    - Между заголовком и тремя карточками видеокадров больше нет никаких лишних разделителей или отступов.
  * **Снекбар в правом верхнем углу**:
    - Добавлен крестик `✕` в правом углу плашки для мгновенного закрытия вручную.
    - Реализовано плавное автоматическое скрытие (fade-out) **через 3 секунды** после сохранения или показа уведомления.
    - При новых действиях или редактировании снекбар мгновенно плавно появляется снова.

- **Удаление линии-разделителя над HERO AREA в боковом меню (`app/admin/page.tsx`)**:
  * Убран `border-b` у верхнего профильного блока сайдбара, теперь переход от профиля к меню `HERO AREA` абсолютно чистый и бесшовный.

- **Стилизация скрытых с сайта видеороликов (`app/admin/page.tsx`)**:
  * Вместо желтых рамок и ярких желтых плашек карточка скрытого видео теперь становится **стильно полупрозрачной** (`opacity-45`, при наведении слегка проявляется до `opacity-85`).
  * Кнопка `СКРЫТ С САЙТА` и бейдж на превью выполнены в сдержанном темном полупрозрачном стиле с белой иконкой и текстом (`text-white/50`).

- **Перемещение кнопки «ВИДЕН НА САЙТЕ / СКРЫТ С САЙТА» под обложку в HERO AREA (`app/admin/page.tsx`)**:
  * В блоке «Главные 5 роликов» кнопка переключения видимости теперь располагается прямо под кадром обложки слева (в единой сетке с разделом «Все работы»).
  * В нижней строке карточки осталась только кнопка удаления (и селектор размера для разработчика).

- **Логика состояний и видимости снекбара (`app/admin/page.tsx`)**:
  * 🟢 **Зеленый круг (Сохранено / Успешные действия)**: автоматически плавно скрывается **через 3 секунды**, крестик закрытия убран (чистый вид по макету).
  * 🔵 **Синий круг (Сохранение / Синхронизация / Загрузка и сжатие)**: отображается **непрерывно** все время, пока идет процесс, без автоскрытия и без крестика.
  * 🟠 **Оранжевый круг (Несохраненные изменения)**: отображается **непрерывно** во время набора текста до момента сохранения, без автоскрытия и без крестика.
  * 🔴 **Красный круг (Ошибка сохранения / Нет связи)**: не скрывается автоматически (чтобы пользователь гарантированно заметил сбой), снабжен крестиком `✕` для ручного закрытия.

- **Усиление прозрачности скрытых карточек и очистка превью (`app/admin/page.tsx`)**:
  * Увеличена прозрачность всей карточки скрытого видео до **25%** (`opacity-25`), при наведении слегка подсвечивается до `opacity-75`.
  * **Удален дублирующий бейдж поверх кадра превью** — теперь информация о скрытии выводится только на кнопке-переключателе под обложкой, превью остается абсолютно чистым.

- **Редизайн подсказки Instagram* по макету (`components/ContactSection.tsx`)**:
  * **Удалена браузерная системная подсказка** (`title="..."`) при наведении на плашку `INSTAGRAM*`.
  * **Сверстана кастомная подсказка точно по макету**:
    - Позиционирование: справа от кнопки `INSTAGRAM*` (`left-full ml-3`).
    - Стиль: чистый белый прямоугольник (`bg-white text-black`), острые углы (`rounded-none`), шрифт `"Geist Mono"` 11px bold uppercase.
    - Текст в 3 строки:
      `*INSTAGRAM ПРИНАДЛЕЖИТ КОМПАНИИ META,`
      `ПРИЗНАННОЙ ЭКСТРЕМИСТСКОЙ ОРГАНИЗАЦИЕЙ`
      `И ЗАПРЕЩЕННОЙ В РФ`

- **Интерактивное меню навигации (ховер и паддинги 8px, `app/page.tsx`)**:
  * **Открытие по ховеру:** всплывающее меню навигации теперь плавно открывается как **при наведении курсора (hover)** на круглую кнопку меню, так и при обычном клике.
  * **Паддинги пунктов меню:** для каждой белой плашки (`КЛИЕНТЫ`, `ПРОЕКТЫ`, `УСЛУГИ`, `ВОПРОСЫ`, `КОНТАКТЫ`) установлены ровные **внутренние отступы 8px** (`padding: 8px`), острые углы `rounded-none` и четкая моноширинная типографика 16px bold uppercase.

- **Построчная авто-подстройка заливки подсказки Instagram* (`components/ContactSection.tsx`)**:
  * Каждая из 3 строк подсказки теперь имеет **свою независимую белую плашку**, ширина которой строго подстраивается под длину конкретной строки (первая строка длинная, вторая еще шире, третья короткая).
  * Увеличен отступ от кнопки `INSTAGRAM*` до подсказки (`ml-6`), чтобы между плашкой и подсказкой был четкий визуальный отступ ровно по скриншоту.

- **Отступ 12px между пунктами меню и кнопкой меню (`app/page.tsx`)**:
  * Установлен точный отступ 12px между нижней плашкой меню (`КОНТАКТЫ`) и круглой кнопкой меню с сохранением плавного перехода курсора мыши.

- **Удаление отступа между строками подсказки Instagram* (`components/ContactSection.tsx`)**:
  * Установлен `gap-0` между строками — белые плашки каждой строки теперь бесшовно прилегают друг к другу в единый ступенчатый блок без черных зазоров.

- **Увеличение яркости скрытых карточек при ховере (`app/admin/page.tsx`)**:
  * В обычном состоянии скрытая карточка приглушена до `opacity-20`, а при наведении курсора становится **полностью видимой и яркой на 100% (`hover:opacity-100`)**, что обеспечивает максимальное удобство редактирования.
  * Убрана серая нативная системная подсказка браузера при ховере на кнопку видимости.

- **Добавление кнопок автоперевода на английский для всех текстовых полей (`app/admin/page.tsx`)**:
  * В блоке **F.A.Q. (Вопросы и ответы)** добавлены белые кнопки перевода во все английские поля:
    1. Текст вопроса (en)
    2. Левая колонка ответа (en)
    3. Правая колонка подробного ответа (en)
  * Также добавлены кнопки перевода во все англоязычные поля в разделах **«Услуги / Процесс»** (фоновый заголовок, верхний текст карточки, главный заголовок, нижний текст) и **«Обо мне»** (верхний и нижний блоки текста).

- **Калибровка таймкодов превью-кадров (исключение белых/пустых кадров концовки, `components/VideoCoverSelector.tsx`)**:
  * Диапазон выборки 24 уникальных стоп-кадров ограничен **золотой кинематографичной зоной [8% - 76%]** от общей длительности ролика.
  * Это полностью исключает попадание в выборку финальных затемнений, уходов в белое (fade-to-white) и пустых титров в самом конце видео.

- **Оптимизация текстов снекбаров под фиксированную ширину 310px (`app/admin/page.tsx`)**:
  * Все системные уведомления и тосты сокращены до лаконичных емких формулировок (`ВСЕ СОХРАНЕНО`, `БЭКАП СКАЧАН`, `БЭКАП ЗАГРУЖЕН`, `ФАЙЛ ЗАГРУЖЕН`, `ВИДЕО СЖАТО`, `РОЛИК ДОБАВЛЕН`, `РАБОТА ДОБАВЛЕНА`, `ПОРЯДОК ИЗМЕНЕН`, `РАЗДЕЛ ДОБАВЛЕН` и т.д.).
  * Тексты гарантированно умещаются в 1 строку плашки шириной 310px без обрезания.

- **Устранение субпиксельных полос между строками подсказки Instagram* (`components/ContactSection.tsx`)**:
  * Добавлен отрицательный верхний отступ `-mt-[1px]` для 2-й и 3-й строк с вертикальным паддингом `py-[2px]`.
  * Белые фоновые прямоугольники строк теперь физически перекрывают друг друга на 1px, полностью устраняя любые субпиксельные зазоры и темные микро-полосы на дисплеях с любым масштабированием.

- **Многоуровневый стек снекбаров (отступ 2px, `app/admin/page.tsx`)**:
  * Когда идет процесс **«ЗАГРУЗКА И СЖАТИЕ...»**, этот снекбар непрерывно висит в верхнем правом углу экрана (`top: 0, right: 0`).
  * Если в этот момент происходят другие события (автосохранение `СОХРАНЕНИЕ...`, успешное действие `СОХРАНЕНО`, тост или ошибка), новый снекбар появляется **строго под верхним с зазором 2px** (`gap: 2px`).

- **Внедрение 5 ключевых функций управления контентом в админке (`app/admin/page.tsx`, `components/VideoCoverSelector.tsx`, `components/DropFileInput.tsx`)**:
  1. **Статичная циклическая сетка Hero `[L, S, M, S, ...]`**: жесткая привязка размеров по индексу слота (`0: L, 1: S, 2: M, 3: S, 4: L...`). При Drag & Drop ролик автоматически адаптирует размер под целевую позицию.
  2. **Покадровый таймлайн-выбор обложки из видео**: добавлена кнопка «ВЫБРАТЬ ИЗ ВИДЕО» с интерактивным видеоплеером, таймлайн-ползунком и микрошагами (`-1.0s`, `-0.1s`, `+0.1s`, `+1.0s`) для сохранения любого кадра.
  3. **Кнопка «ДОБАВИТЬ НА ГЛАВНУЮ» в разделе «Все работы»**: модальное окно выбора целевой позиции (Позиция #1 L, #2 S, #3 M, #4 S, #5 L или в конец списка).
  4. **Лимит текста (45 символов) с валидацией**: подсчет оптимальной длины строки карточек (45 символов) с выводом счетчика символов и красного предупреждения при превышении.
  5. **Валидация веса видео (лимит 150 МБ)**: вывод информативной ошибки под полем `DropFileInput` при загрузке чрезмерно тяжелых файлов.

- **Обновление стилей подсказки Instagram* (`components/ContactSection.tsx`)**:
  * Цвет текста установлен `#E6E6E6`.
  * Цвет фона плашек установлен `#3A3A3A`.
  * Добавлен дополнительный внутренний отступ справа +2px (`paddingRight: '10px'`).

- **Установка отступов плашки Instagram* (`components/ContactSection.tsx`)**:
  * Паддинги всех трех строк установлены ровно по 1px (`padding: '1px'`), формируя максимально аккуратную компактную обертку.

- **Инверсия ховеров переключателя языка и отступ меню (`app/page.tsx`, `components/Sidebar.tsx`)**:
  * **Отступ всплывающего меню навигации:** зазор над круглой кнопкой бургера увеличен до точных **20px** (`pb-[20px]`).
  * **Переключатель RU / EN:** 
    - При наведении на активную синюю плашку фон становится белым, а текст темным (`hover:bg-white hover:text-[#0B0B0B]`).
    - При наведении на неактивную белую плашку фон становится синим, а текст белым (`hover:bg-[#1458E6] hover:text-white`).

- **Обновление кнопки выбора стоп-кадра (`components/VideoCoverSelector.tsx`)**:
  * Название кнопки изменено на **«ВЫБРАТЬ СТОПКАДР»** (в открытом состоянии — **«ЗАКРЫТЬ СТОПКАДР»**).
  * Иконка заменена на иконку картинки/изображения (`ImageIcon`).

- **Редизайн интерактивного таймлайна выбора стоп-кадра (`components/VideoCoverSelector.tsx`)**:
  * Полностью убран весь лишний текст и шапка.
  * В правом верхнем углу видео размещена синяя квадратная кнопка с галочкой (`Check`) для подтверждения и применения кадра.
  * Слева по центру видео размещены две круглые плашки покадровой перемотки назад (`-0.1s` и `-1s`).
  * Справа по центру видео размещены две круглые плашки покадровой перемотки вперед (`+0.1s` и `+1s`).
  * Снизу реализована аккуратная плоская шкала таймлайна `#3A3A3A` с синим индикатором текущего кадра `#1458E6`.

- **Доработка стилей стоп-кадра (`components/VideoCoverSelector.tsx`)**:
  * Фон сделан прозрачным с аккуратной контурной рамкой `border border-[#26282C]`.
  * Плашки секунд (`-0.1s`, `-1s`, `+0.1s`, `+1s`) уменьшены и точно выровнены по бокам.
  * Бегунок/курсор на шкале таймлайна сделан строго квадратным (`18×18px` синий квадрат `#1458E6`).

- **Точное позиционирование плашек перемотки стоп-кадра (`components/VideoCoverSelector.tsx`)**:
  * Слева плашки (`-0.1s` и `-1s`) выровнены по правому краю друг относительно друга с отступом ровно 8px (`mr-2`) от левого края видео.
  * Справа плашки (`+0.1s` и `+1s`) выровнены по левому краю друг относительно друга с отступом ровно 8px (`ml-2`) от правого края видео.
  * Плашки имеют сплошную заливку `#18181A` без обводки (`border-none`) с горизонтальным паддингом по 4px (`padding: 2px 4px`).

- **Перемещение кнопки «ДОБАВИТЬ НА ГЛАВНУЮ» (`app/admin/page.tsx`)**:
  * Синяя плашка «ДОБАВИТЬ НА ГЛАВНУЮ» перемещена в левую колонку карточки прямо под кнопку видимости обложки («ВИДЕН НА САЙТЕ»).
  * В правом нижнем углу карточки осталась только кнопка «УДАЛИТЬ».

- **Установка отступов плашек времени стоп-кадра (`components/VideoCoverSelector.tsx`)**:
  * Установлены точные внешние отступы ровно по 8px слева и справа (`marginRight: '8px'` для левых кнопок и `marginLeft: '8px'` для правых кнопок относительно границ видео).

- **Группировка кнопок управления карточкой проекта (`app/admin/page.tsx`)**:
  * Все 3 кнопки перенесены в левую колонку под видео/обложку в строгом порядке:
    1. **`✨ ДОБАВИТЬ НА ГЛАВНУЮ`** (синяя акцентная плашка)
    2. **`👁 ВИДЕН НА САЙТЕ` / `СКРЫТ С САЙТА`** (темная кнопка видимости)
    3. **`УДАЛИТЬ`** (кнопка удаления с подтверждением)
  * Нижний ряд кнопок убран, карточка стала компактнее и чище.

- **Унификация кнопок удаления и перенос в колонку обложки Hero (`app/admin/page.tsx`)**:
  * В секции главной страницы (Hero) кнопка «УДАЛИТЬ» перенесена в левую колонку под кнопку «ВИДЕН НА САЙТЕ».
  * К кнопке «УДАЛИТЬ» добавлена иконка корзины (`Trash2`) как в секции Hero, так и в секции «Все работы».
  * Все карточки приведены к единому компактному стандарту.

- **Синхронизация порядка пунктов меню в админке с сайтом (`app/admin/page.tsx`)**:
  * Пункты меню сайдбара админки выстроены в строгой последовательности блоков сайта:
    1. **HERO AREA**
    2. **КЛИЕНТЫ**
    3. **ВСЕ РАБОТЫ**
    4. **УСЛУГИ**
    5. **ОБО МНЕ**
    6. **F.A.Q.**
    7. **НАСТРОЙКИ**

- **Инструмент обрезки обложек и фикс кнопок (`components/ImageCropModal.tsx`, `components/VideoCoverSelector.tsx`, `app/admin/page.tsx`)**:
  * Создан интерактивный компонент `ImageCropModal` с поддержкой предустановленных пропорций: `16:9` (горизонтальное видео), `9:16` (Reels/вертикальное видео), `1:1` (квадрат), `Свободный`, контролем масштабирования (`+`/`-`), смещением рамки и авто-извлечением кадра из видео на canvas с сохранением в высоком разрешении.
  * В панель обложек добавлена кнопка **«ОБРЕЗАТЬ»** с иконкой `Crop`.
  * Возвращена яркая белая обводка (`border-white ring-white/80`) для активного выбранного стоп-кадра.
  * Исправлена кнопка «ДОБАВИТЬ НА ГЛАВНУЮ»: убран перенос и сплющивание текста, оптимизированы отступы и ширина колонки превью (`w-44`/`w-48`).

- **Исправление пропорций и сетки Hero роликов (`app/admin/page.tsx`, `components/ReelsSection.tsx`)**:
  * В паттерне сетки Hero (`L -> S -> M -> S`) размер `M` (позиция #3) был скорректирован с некорректного квадрата `474×474` на правильный широкоформатный размер из дизайна **`818×460` (16:9)**, а `S` — на **`556×338`**.
  * В компоненте `ReelsSection` добавлен динамический `aspectRatio: ${reel.width} / ${reel.height}`, исключающий обрезку или сжатие видео на любых разрешениях экрана.

- **Добавление белой рамки вокруг области выбора стоп-кадра (`components/VideoCoverSelector.tsx`)**:
  * Вокруг всего блока выбора стоп-кадра добавлена тонкая белая рамка (`border border-white/70 shadow-lg`), а также разделяющая линия `border-b border-white/40` между областью видео и нижней шкалой таймлайна в точном соответствии с макетом.

- **Интерактивные ручки масштабирования и перетаскивания рамки обрезки (`components/ImageCropModal.tsx`)**:
  * Добавлены 4 угловых маркера (`nw`, `ne`, `se`, `sw`) и 4 боковых маркера (`n`, `s`, `e`, `w`) с визуальными белыми ручками.
  * Реализовано плавное перетаскивание мышью/тачем (drag & resize) за любой уголок и перемещение всей рамки за центральную область с сохранением пропорций и границами изображения.

- **Замена кнопки «Тест» на сплошную синюю кнопку «ОБРЕЗАТЬ» (`app/admin/page.tsx`)**:
  * В карточках проектов и Hero при наведении на превью обложки вместо кнопки воспроизведения «Тест» теперь отображается акцентная сплошная синяя плашка **`ОБРЕЗАТЬ`** с иконкой `Crop`.
  * По клику мгновенно открывается интерактивное модальное окно обрезки с пропорциями (16:9, 9:16, 1:1, свободный) и живым drag & drop кадрированием.

- **Унификация ширины колонки превью (`lg:w-48`) и возврат стандартного размера кнопок (`app/admin/page.tsx`)**:
  * Во всех карточках проектов (включая вертикальные видео) ширина левой колонки с превью установлена в единый стандарт `lg:w-48`.
  * Возвращены стандартные полноразмерные кнопки `h-[36px]` с нормальным шрифтом `text-[12px]`, текст больше не сжимается и не ломается.

- **Очистка верхней панели выбора обложек (`components/VideoCoverSelector.tsx`)**:
  * Кнопка «ОБРЕЗАТЬ» убрана из верхнего ряда панели действий над обложками, так как теперь обрезка вызывается напрямую при наведении на превью обложки синей кнопкой «ОБРЕЗАТЬ».

- **Стилизация кнопки перевода (`components/TranslateButton.tsx`)**:
  * По умолчанию цвет кнопки установлен в `#323232` с белой иконкой.
  * При наведении (hover) кнопка плавно переключается на белую заливку с черной иконкой (`hover:bg-white hover:text-black`).

- **Увеличение кегля заголовков разделов админки в 2 раза (`app/admin/page.tsx`)**:
  * Кегль главных заголовков разделов в правой панели админки увеличен с `20px` до **`40px`** (`text-[32px] md:text-[38px] lg:text-[40px] font-bold tracking-[-0.4px]`).

- **Единая ширина (`lg:w-48`) для горизонтальных и вертикальных обложек (`app/admin/page.tsx`)**:
  * И у горизонтальных видео (16:9), и у вертикальных Reels (9:16) ширина карточки/превью строго одинаковая (`w-full lg:w-48`), а высота адаптируется под их естественные пропорции.

- **Унификация всех торцевых кнопок в полях ввода (`#323232` -> белый по hover)**:
  * В `TranslateButton.tsx`, `DropFileInput.tsx` (скрепка загрузки файлов видео, фото, логотипов) и кнопках инпутов разделов/фото `app/admin/page.tsx`:
    - По умолчанию: цвет фона `#323232` с белой иконкой;
    - При наведении (hover): белая заливка с контрастной черной иконкой (`hover:bg-white hover:text-black`).

- **Увеличение нижнего отступа (pb-220px) во всех разделах админки (`app/admin/page.tsx`)**:
  * Нижний паддинг основного скроллируемого контейнера увеличен до `220px` (`pb-[220px]`).
  * Теперь при прокрутке до конца в разделах «Клиенты», «Все работы», «Hero» и других последняя карточка и её инпуты полностью видны и больше не перекрываются плавающими нижними кнопками («+» и «СОХРАНИТЬ»).

- **Обновление и лаконизация заголовков разделов админки (`app/admin/page.tsx`)**:
  * `1` — `HERO AREA`
  * `2` — `КЛИЕНТЫ И ЛОГО`
  * `3` — `ВСЕ РАБОТЫ`
  * `4` — `УСЛУГИ`
  * `5` — `ОБО МНЕ`
  * `6` — `ВОПРОСЫ`
  * `7` — `НАСТРОЙКИ`
  * Синхронизированы пункты в левом меню и главные заголовки в правой рабочей области.

- **Переименование пункта меню админки (`app/admin/page.tsx`)**:
  * В левом сайдбаре пункт `КЛИЕНТЫ И ЛОГО` переименован в `КЛИЕНТЫ`.

- **Оптимизация логики автосохранения и уведомлений (`app/admin/page.tsx`)**:
  * Убран промежуточный мерцающий снэкбар «НЕСОХРАНЕННЫЕ ИЗМЕНЕНИЯ» при наборе текста и редактировании.
  * Теперь при изменении контента система тихо аккумулирует изменения, а после завершения автосохранения плавно показывает аккуратное подтверждение `СОХРАНЕНО В HH:MM:SS`, которое мягко исчезает через 2.5 секунды.

- **Рамка таймлайна и кнопка отмены выбора стоп-кадра (`components/VideoCoverSelector.tsx`)**:
  * Обводка контейнера выбора стоп-кадра приведена к стандартному стилю инпутов (`border border-[#26282C]`).
  * В правом верхнем углу области стоп-кадра под синей кнопкой подтверждения `✓` добавлена темная кнопка отмены `✕` (`bg-[#323232] hover:bg-white`), которая сворачивает панель без сохранения.

- **Расположение поля кастомной обложки и ренейминг кнопок карточек (`VideoCoverSelector.tsx`, `app/admin/page.tsx`)**:
  * Поле ввода/загрузки файла при нажатии «ВЫБРАТЬ СВОЮ» теперь появляется **выше** 3-х предложенных стоп-кадров.
  * Кнопки карточек переименованы в лаконичный формат:
    - `НА ГЛАВНУЮ` (вместо `ДОБАВИТЬ НА ГЛАВНУЮ`)
    - `СКРЫТЬ` (если элемент виден) и `ПОКАЗАТЬ` (если скрыт).

- **Восстановление точных размеров сетки Hero из бэкапов (`app/admin/page.tsx`, `data/content.json`)**:
  * Размер **`M` (Позиция #3)** восстановлен до эталонного **`818×460`** (вместо устаревшего квадрата `474×474`).
  * Размер **`S` (Позиции #2 и #4)** приведен к эталонному **`557×313`**.
  * Размер **`L` (Позиции #1 и #5)** — **`964×542`**.
  * Добавлена автоматическая нормализация сетки `applyHeroGridPattern` при загрузке данных, чтобы любые старые закэшированные значения в браузере автоматически обновлялись на актуальные размеры пресетов.

- **Оптимизация видео и подсказка макс. размера в разделе «Клиенты» (`app/admin/page.tsx`)**:
  * Для поля загрузки видео клиента подключена автоматическая транскодация / оптимизация видео (`handleFileUpload` -> `/api/upload` через FFmpeg libx264 + faststart).
  * Добавлена текстовая подсказка о рекомендуемом формате и лимите размера: `Рекомендуемый формат: MP4 (H.264), вес до 150 МБ. Видео автоматически оптимизируется при загрузке.`
  * Подключено отображение ошибок валидации размера файлов.

- **Синхронизация пропорций и размеров Hero-роликов на главной странице (`components/ReelsSection.tsx`)**:
  * В `ReelsSection.tsx` внедрена динамическая нормализация сетки `HERO_GRID_PATTERN` (`L: 964×542`, `S: 557×313`, `M: 818×460`, `S: 557×313`).
  * Теперь на главной странице позиция **#3 (M)** гарантированно отображается в эталонном широкоформатном соотношении **`818×460` (16:9)**, без зависимости от старых данных кэша.
  * Также на главной странице добавлена фильтрация скрытых (`hidden`) роликов.

- **Поддержка загрузки и сжатия больших видеофайлов до 3 ГБ во всех разделах, включая «Клиенты» (`app/admin/page.tsx`, `api/upload/route.ts`)**:
  * Снято искусственное клиентское ограничение в 150 МБ — лимит повышен до **3000 МБ (3 ГБ)**.
  * Любое видео (даже большие файлы из камеры, ProRes, MOV или тяжелые MP4) теперь без ошибок отправляется на сервер и автоматически оптимизируется через FFmpeg libx264 с параметром `+faststart`.
  * Обновлены подсказки под полями загрузки во всех разделах.

- **Редактирование заголовка блока «Контакты» (`app/admin/page.tsx`, `components/ContactSection.tsx`, `lib/supabase.ts`)**:
  * В интерфейс настроек (`SiteSettings`) добавлены поля `contacts_title_ru` и `contacts_title_en`.
  * В админ-панели (вкладка **«Настройки»** -> **«Контакты и социальные сети»**) добавлены многострочные поля для настройки заголовка на русском и английском языках с кнопкой быстрого перевода.
  * В компоненте `ContactSection.tsx` подключена динамическая привязка к настройкам с сохранением многострочных переносов строк (`whitespace-pre-line`).

- **Реализация управления видеоплеером в стиле YouTube (`components/VideoModal.tsx`)**:
  * **Стрелки `↑` / `↓`**: регулировка громкости шагом 10%, с автоматическим включением звука (unmute) и отображением процентов громкости (`10%`, `50%`, `100%`).
  * **Клавиша `M` (или русская `Ь`)**: моментальное включение / отключение звука (Mute/Unmute).
  * **Клавиша `Space` / `K` (или `Л`)**: пауза и продолжение воспроизведения.
  * **Стрелки `←` / `→` (или `J` / `L`)**: быстрая перемотка на `-5 сек` / `+5 сек`.
  * **Клавиша `F` (или `А`)**: переход в полноэкранный режим и выход из него.
  * **Клавиша `Escape`**: выход из полноэкранного режима или закрытие попапа.
  * Добавлен всплывающий анимированный HUD-индикатор по центру экрана с иконкой и текстом действия (громкость, mute, пауза, перемотка).

- **Фиксация нижнего паддинга меню навигации (`app/page.tsx`)**:
  * Для контейнера пунктов выпадающего меню явно зафиксирован инлайн-стиль `style={{ paddingBottom: '20px' }}` (ровно 20px от нижнего пункта «КОНТАКТЫ» до кнопки-бургера).

- **Окно входа с выбором пользователя и разграничением прав (`app/admin/page.tsx`)**:
  * На экране логина добавлены вкладки выбора пользователя:
    - 👩‍💻 **АЛЕВТИНА** (`alevtina`): Главный разработчик (полный доступ, системные настройки, экспорт/импорт бэкапов). Аватар `AL` (синий).
    - 🎬 **ВЛАД САПУНОВ** (`vlad`): Редактор контента (управление видео, клиентами, вопросами). Аватар `VS`.
  * При переключении вкладок динамически обновляются аватар, имя, роль и подсказка.
  * Прямой вход: можно просто ввести `alevtina` (для разработчика) или `2026` / `vlad` (для редактора).
  * В сайдбаре кнопка выхода переименована в «СМЕНИТЬ ПОЛЬЗОВАТЕЛЯ».

- **Разделение вкладок «Контакты» и «Настройки» и кнопка «Выйти» (`app/admin/page.tsx`)**:
  * В сайдбаре кнопка выхода переименована в **«ВЫЙТИ»**.
  * Разделены разделы:
    - **`КОНТАКТЫ`**: отдельная вкладка для редактирования заголовка блока контактов и всех социальных сетей (Telegram, Behance, YouTube, Instagram*, Email, Телефон).
    - **`НАСТРОЙКИ`**: отдельная вкладка только для главного разработчика (управление PIN-кодом, экспорт и импорт полных JSON-бэкапов базы данных).

- **Улучшение карточки бэкапов в разделе «Настройки» (`app/admin/page.tsx`)**:
  * Убран лишний текст описания и угловой бейдж `JSON BACKUP`.
  * У обеих кнопок установлен горизонтальный паддинг ровно `20px` (`style={{ paddingLeft: '20px', paddingRight: '20px' }}`).
  * У кнопки «Восстановить из бэкапа» убрана рамка (`border-none`).

- **Очистка PIN-кода и сброс на HERO AREA при входе/выходе (`app/admin/page.tsx`)**:
  * При выходе из аккаунта и при переключении пользователя поле PIN-кода теперь полностью очищается.
  * При каждом новом входе под любым пользователем (Алевтина или Влад) админка всегда открывается на главной вкладке (`HERO AREA`).

- **Добавлен нижний псевдоотступ (100px spacer) в раздел «Клиенты» (`app/admin/page.tsx`)**:
  * В конце списка клиентов добавлен `<div className="h-[100px] w-full shrink-0" />`, благодаря чему плавающая нижняя кнопка `[+] [СОХРАНИТЬ]` больше не перекрывает нижнюю карточку и инпуты.

- **Комплексная оптимизация UX/UI, Performance, SEO и Аналитика**:
  * **1.1 & 1.3 Motion & Transitions**:
    - Умный автоплей превью при наведении с задержкой `180ms` (защита от фликера при быстром скролле);
    - Кинематографичное открытие модального окна видео с масштабированием `zoom-in-95` и плавным `backdrop-blur`.
  * **2.1 & 2.2 & 2.3 Performance & Media Streaming**:
    - Подключение `IntersectionObserver` в `ReelsSection` и `WorksSection`: видео декодируются и играют только при входе в видимую область экрана (`rootMargin: '100px 0px'`);
    - Поддержка форматов `image/avif` и `image/webp` в `next.config.ts`;
    - Замена `preload="auto"` на легковесный `preload="metadata"` + `playsInline` (снижение расхода трафика и моментальная готовность таймлайна).
  * **3.2 Mobile Thumb Zone**:
    - Адаптивное позиционирование плавающего навигационного бара (бургер + «СВЯЗАТЬСЯ») в нижней зоне большого пальца на смартфонах (`left-[16px] bottom-[16px]`).
  * **5.2 Аналитика просмотров (`/api/analytics` + Раздел «АНАЛИТИКА» в админке)**:
    - Автоматическая фиксация каждого просмотра/запуска видео;
    - Раздел «АНАЛИТИКА» в админ-панели с KPI-карточками (Всего просмотров, Проектов в ротации, Лидер просмотров) и рейтинговой таблицей видео с прогресс-барами.
  * **SEO-продвижение**:
    - Мета-теги OpenGraph, Twitter Cards, keywords, canonical URL;
    - Микроразметка Schema.org JSON-LD (`Person` & `ProfessionalService`);
    - Автогенерация `sitemap.xml` и `robots.txt`.

- **Корректировки меню и откат изменений**:
  * Всплывающие пункты навигационного меню: внутренний отступ уменьшен с `8px` до ровно **`4px`** (`padding: '4px'`).
  * Пункт 1.1 откачен назад: воспроизведение превью видео в ленте и во всех работах возвращено к исходному плавному циклическому режиму.
  * Пункт 1.3 возвращен к исходному стандартному модальному представлению плеера.

- **Аналитика как главный раздел №1 + Трекинг кликов по связи**:
  * Раздел **`АНАЛИТИКА`** стал главной первой вкладкой в админке для всех ролей (Алевтина и Влад) — при входе по умолчанию открывается аналитика.
  * Добавлен трекинг кликов по всем кнопкам связи:
    - `Telegram`, `Телефон`, `Email`, `Behance`, `YouTube`, `Instagram*` в блоке контактов и в сайдбаре;
    - Плавающая кнопка «СВЯЗАТЬСЯ».
  * В админ-панели внедрена карточка **«Клики по кнопкам связи и конверсия в лиды»** с визуальными прогресс-барами и фиксацией времени каждого клика.

- **Обновление паддингов пунктов навигационного меню (`app/page.tsx`)**:
  * Внутренний отступ у всех бейджей всплывающего меню установлен ровно **`6px`** (`padding: '6px'`).

- **Прямая ссылка на Telegram на кнопке «СВЯЗАТЬСЯ» (`app/page.tsx`)**:
  * Плавающая синяя кнопка «СВЯЗАТЬСЯ» теперь сразу открывает Telegram пользователя (`settings.telegram` / `https://t.me/sapunov_vlad`) в новой вкладке и автоматически фиксирует клик в аналитике лидов.

- **Поле для ссылки кнопки «СВЯЗАТЬСЯ» во вкладке «КОНТАКТЫ» (`app/admin/page.tsx`, `lib/supabase.ts`, `data/content.json`)**:
  * Во вкладке **КОНТАКТЫ** админ-панели добавлено поле **«кнопка «связаться» (ссылка перехода)»**.
  * Теперь можно указать любую ссылку (Telegram, WhatsApp, форму заявки и т.д.), и плавающая кнопка «СВЯЗАТЬСЯ» на сайте будет вести именно по ней.

- **Обновление структуры навигационного меню (`app/page.tsx`)**:
  * Пункты **ПРОЕКТЫ** и **КЛИЕНТЫ** поменяны местами (теперь первым идет «ПРОЕКТЫ», вторым «КЛИЕНТЫ»).
  * Пункт **ВОПРОСЫ** полностью исключен из всплывающего меню навигации.
  * Актуальный состав меню: **ПРОЕКТЫ**, **КЛИЕНТЫ**, **УСЛУГИ**, **КОНТАКТЫ**.

- **Исправление и авто-валидация ссылок Telegram (`lib/supabase.ts`, `data/content.json`, `app/page.tsx`, `components/ContactSection.tsx`)**:
  * Причина перенаправления на `telegram.org`: в ссылке был указан символ `@` (`https://t.me/@ElllRate`), который серверами Telegram не распознается как валидный юзернейм и редиректит на главную страницу.
  * Исправлено на `https://t.me/ElllRate`.
  * Реализована авто-нормализация `formatExternalUrl`: любой формат ввода (`@username`, `t.me/@username`, `telegram.me/@username`, `username`) автоматически преобразуется в правильную рабочую ссылку `https://t.me/username`.

- **Обновление админки и модального видеоплеера (2026-08-31)**:
  * 1. В карточках видео (Hero и Проекты) блок выбора обложки перемещен на первое место над полями названий.
  * 2. Длинные пути `/uploads/1788...` заменены на карточку файла: иконка типа файла, чистое имя (без таймстемпов) и вес файла (например, `MP4 • 42.8 MB`), с быстрыми кнопками замены/очистки.
  * 3. Во вкладке «Аналитика» скрыта кнопка «СОХРАНИТЬ».
  * 4. Системные окна `window.confirm` заменены на кастомные модальные окна подтверждения по центру экрана (Glassmorphism, темная тема).
  * 5. В видеоплеере на главной странице добавлены плавающие стрелки влево/вправо для быстрого переключения между роликами плейлиста и горячие клавиши.

- **Редизайн формы входа в админ-панель (2026-08-31)**:
  * Реализован новый минималистичный интерфейс по макету:
    - Заголовок `ВХОД` вверху формы (белый шрифт, моноширинный).
    - Поле ввода `логин` с плейсхолдером `НАПРИМЕР , VLAD`.
    - Поле ввода `пин-код` с кнопкой переключения видимости (иконка глаза).
    - Кнопки снизу: закругленная серая `ОТКРЫТЬ САЙТ` и акцентная синяя `ВОЙТИ`.

- **Обновление формы входа, профилей, аватарок и управления доступом (2026-08-31)**:
  * 1. Исправлена подсветка инпутов в форме входа: убраны синие контуры при фокусе, применен строгий нейтральный стиль (`border-[#26282C] focus:border-[#444]`).
  * 2. Полностью убрано свечение (тени/glow) с кнопки «ВОЙТИ» и зафиксировано глобальное правило отказа от светящихся кнопок на проекте.
  * 3. Увеличен отступ между нижними pill-кнопками формы входа (`gap-5`).
  * 4. Вкладка «НАСТРОЙКИ» открыта для всех авторизованных пользователей. Добавлен блок «МОЙ ПРОФИЛЬ И PIN-КОД»: смена имени, логина, PIN-кода и загрузка/удаление кастомной аватарки с превью в сайдбаре.
  * 5. В режиме главного разработчика реализован блок «УПРАВЛЕНИЕ РЕДАКТОРАМИ И ДОСТУПОМ»: добавление редакторов, редактирование их PIN-кодов/логинов и удаление с подтверждением.

- **Редизайн модального окна удаления / подтверждения (2026-08-31)**:
  * Окно удаления полностью обновлено в точности по новому макету:
    - Убрана рамка (обводка), оставлен только чистый темный фон карточки `#141416` с глубокой тенью и блюром подложки.
    - Центрированный заголовок белым моноширинным шрифтом (например, `УДАЛЕНИЕ ВИДЕО`).
    - Описание действия серым приглушенным текстом по центру (`#7A7C85`).
    - Закругленные pill-кнопки (без свечения): темная `ОТМЕНА` и ярко-красная `УДАЛИТЬ` (`#FF0000`).

- **Микро-корректировки компонента прикрепленных файлов `DropFileInput` (2026-08-31)**:
  * 1. Установлен внутренний отступ слева (от левого края до иконки) ровно 8px (`paddingLeft: '8px'`).
  * 2. У иконки видео/изображения полностью убрана обводка (`border-none bg-transparent`).
  * 3. Установлен отступ справа (от крестика до правого края) ровно 8px (`paddingRight: '8px'`).
  * 4. Расстояние между иконкой ссылки и крестиком настроено на 4px (`gap-[4px]`).
  * 5. У кнопок ссылки и удаления убрана фоновая заливка при наведении: иконка ссылки при ховере становится чисто белой (`hover:text-white`), а крестик — чисто красным (`hover:text-[#FF0000]`).

- **Уточнение стилей DropFileInput (2026-08-31)**:
  * 1. Фон иконки типа файла возвращен (`bg-[#232326] rounded`), обводка удалена (`border-none`).
  * 2. Для крестика удаления задан левый отступ 8px (`paddingLeft: '8px'`).

- **Точная подгонка типографики и отступов окон подтверждения и авторизации по Figma (2026-08-31)**:
  * 1. Отступ между заголовком и описанием в окне удаления установлен ровно 12px (`marginTop: '12px'`).
  * 2. Между кнопками задан нулевой зазор (`gap: 0` / кнопки прилегают вплотную).
  * 3. Внешний отступ кнопок сверху/снизу установлен в 32px (`marginTop: 32px`).
  * 4. Внутренние паддинги кнопок: слева и справа по 16px, сверху и снизу по 8px (`padding: '8px 16px'`).
  * 5. Стили шрифтов приведены в строгое соответствие с Figma Inspect:
    - Заголовок (text/M): `Geist Mono`, 20px, bold 700, line-height 110% (22px), letter-spacing -0.2px, uppercase.
    - Описание (text/XS): `Geist Mono`, 14px, bold 700, line-height 125% (17.5px), letter-spacing -0.14px, lowercase, opacity 0.4.
    - Кнопки (text/S): `Geist Mono`, 16px, bold 700, line-height 125% (20px), letter-spacing -0.16px, uppercase.
  * 6. Те же правила типографики, паддингов и расположения кнопок синхронно применены к форме входа/авторизации.

- **Удаление скруглений (border-radius) у обложек и стопкадров видео (2026-08-31)**:
  * У карточек выбора обложек в `VideoCoverSelector` скругления заменены на строгие прямые углы (`rounded-none`).
  * У бейджей с таймкодом длительности видео также убрано скругление (`rounded-none`).
  * У миниатюр превью видео в админке (блоки Hero и Работы) скругления удалены (`rounded-none`).

- **Добавлен выносной квадратный крестик закрытия для окон подтверждения (2026-08-31)**:
  * В правой верхней части модального окна добавлен плавающий квадратный блок (`40x40px`) с крестиком.
  * Блок вынесен за границу основного окна с отступом ровно 4px (`left: calc(100% + 4px)`).
  * Оформление совпадает с квадратными кнопками инпутов: темный фон `#1E1E22`, при наведении становится белым с черным крестиком.

- **Обновление отступов иконок DropFileInput и цвета выносного крестика (2026-08-31)**:
  * В `DropFileInput` вокруг крестика настроены симметричные отступы по 4px: 4px слева (зазор от иконки ссылки) и 4px справа (до границы кнопки прикрепления).
  * Для плавающей квадратной кнопки закрытия модальных окон подтверждения цвет фона синхронизирован с фоном основной формы (`#141416`).

- **Исправление артефакта полосы в поле PIN-кода (2026-08-31)**:
  * Удален внутренний темный фон и левая разделительная граница у кнопки показа/скрытия пароля в поле ПИН-кода (`bg-transparent border-none`).
  * Теперь при фокусе и в обычном состоянии рамка поля ввода выглядит монолитно и равномерно, без лишних вертикальных полос справа.

- **Обновление отступов иконок в инпутах до 12px (2026-08-31)**:
  * В `DropFileInput` вокруг крестика установлены отступы ровно по 12px: 12px слева (до иконки ссылки) и 12px справа (до границы кнопки скрепки).
  * Во всех инпутах с кнопками действий (включая глазик в поле PIN-кода) правый отступ приведен к 12px (`pr-[12px]`).

- **Глобальная унификация инпутов: белая подсветка при фокусе и бесшовные кнопки действий (2026-08-31)**:
  * Все активные поля ввода (`input`, `textarea`, обертки полей) во всей админке и на сайте теперь подсвечиваются строгим белым цветом (`focus:border-white`, `focus-within:border-white`), синяя подсветка полностью исключена.
  * Правые квадратные кнопки действий (кнопка сохранения названия галочкой, перевод `TranslateButton`, прикрепление файлов скрепкой) выровнены со смещением `-my-[1px] -mr-[1px]` и без внутренних рамок, что исключает полосы и наложения рамок на правом краю.

- **Полное устранение правой полосы на полях с кнопками действий (`border-r-0`) (2026-08-31)**:
  * Во всех контейнерах инпутов с прикрепленными правыми кнопками (кнопка перевода `TranslateButton`, галочка сохранения названия раздела, скрепка в `DropFileInput` и фото) установлена директива `border-r-0`.
  * Правая граница полностью убрана, благодаря чему квадратная кнопка действия бесшовно завершает поле ввода без каких-либо артефактов и белых полос справа при фокусе.

- **Редизайн страницы аналитики под макеты (2026-08-31)**:
  * Верхние 4 карточки KPI унифицированы: маленький верхний подзаголовок, полупрозрачные белые/серые иконки (`text-white/40`), крупная цифра и под ней поясняющий текст (`запусков`, `переходов`, `проектов`, название топ-ролика).
  * Заголовки разделов «КЛИКИ ПО КНОПКАМ И КОНВЕРСИЯ В ЛИДЫ» и «РЕЙТИНГ ПРОСМОТРА ВИДЕО» вынесены на фон страницы без карточек-оберток.
  * Каждый элемент списка (кнопки связи, видеоролики) оформлен отдельной карточкой с правым блоком даты последнего действия (`последний клик:`, `последний просмотр:`) и синей плашкой-бейджем (`2 КЛИКА`, `12 👁`).

- **Унификация карточек клиентов с разделами Главная (Hero) и Работы (Works) (2026-08-31)**:
  * Карточка каждого клиента переведена на двухколоночную компоновку (`rounded-[24px] flex flex-col md:flex-row gap-6`).
  * Слева расположен блок предпросмотра (логотип, название клиента, статус видимости логотипа и ручка перетаскивания) и кнопки действий под ним (`ЛОГОТИП ВКЛЮЧЕН`, `СКРЫТЬ/ПОКАЗАТЬ`, `УДАЛИТЬ`).
  * Справа расположены все поля ввода (название RU/EN, загрузка логотипа и видео проекта).

- **Обновление бейджа файла в поле загрузки (2026-08-31)**:
  * Иконка файла в `DropFileInput` переведена из синего цвета в чистый белый (`text-white`).
  * Закругления у подложки иконки убраны (`rounded-none`), фон `#232326` сохранен.

- **Правки аналитики и включение выделения текста (2026-08-31)**:
  * В аналитике во всех синих тегах (бейджах) установлены точные отступы 4px сверху/снизу и 8px слева/справа (`padding: 4px 8px`).
  * В теге кликов заменено слово «кликов» на иконку клика мыши (`MousePointerClick`).
  * Убрана надпись «Всего: ...» справа от заголовка кликов.
  * Кнопки «Обновить» и «Сбросить» добавлены одинаково в обе секции аналитики.
  * В карточке «Лидер просмотров» (топ проект) добавлено четкое пояснение значения (например, «18 просмотров» и название проекта под ним).
  * У карточек списка кликов и просмотров паддинги слева и справа выставлены ровно по 16px.
  * Удален класс `select-none` со всех разделов сайта и админ-панели — теперь весь текст можно свободно выделять и копировать.

- **Удаление нижнего псевдоотступа на странице аналитики (2026-08-31)**:
  * Удален нижний блок-спейсер `h-[100px]` в табе аналитики, так как там отсутствуют плавающие кнопки сохранения и дополнительный отступ снизу не требуется.

- **Редизайн карточки клиента и обновление состояния активных инпутов (2026-08-31)**:
  * Убран лишний фон и обводка у плашки превью логотипа в карточке клиента.
  * Кнопки переименованы в «ВКЛЮЧИТЬ/ВЫКЛЮЧИТЬ ЛОГОТИП» и «ПОКАЗАТЬ/СКРЫТЬ КАРТОЧКУ», синий цвет заменен на нейтральный серый `#232326`.
  * Активные инпуты переведены со слепящей белой рамки на мягкую подсветку фона (`bg-white/[0.04]`), сохраняя тонкую базовую границу `#26282C` без стыков.

- **Минималистичные иконки типов файлов в полях загрузки (2026-08-31)**:
  * Заменены иконки: для видео теперь используется чистый значок `Play`, для изображений — лаконичная `Image`.
  * Цвет иконок переведен из белого в спокойный полупрозрачный серый `#8C8E96` (`text-white/50`).

- **Плавающая панель действий в аналитике и исправление кнопок перевода (2026-08-31)**:
  * В аналитике кнопки вынесены в единую плавающую панель по центру экрана снизу: слева белая кнопка `СБРОСИТЬ`, справа синяя `ОБНОВИТЬ` с отступом 0 (`gap-0`) между ними.
  * Убраны дублирующиеся кнопки из заголовков списков кликов и просмотров.
  * Возвращен нижний отступ-спейсер `100px` в аналитике для плавного скролла под плавающими кнопками.
  * Исправлены кнопки перевода: строго зафиксирован квадратный размер `40×40px`, добавлены аккуратные разделители `border-l` и `border-b`, возвращены полные 4-сторонние границы инпутов.

- **Типографическая чистка: удаление пробелов перед двоеточиями и запятыми (2026-08-31)**:
  * Проведена ревизия всех текстов, меток и плейсхолдеров сайта и админки.
  * Убраны лишние пробелы перед двоеточиями (`последний просмотр:`, `последний клик:`, `КАРТИНКА УРОВНЯ КИНО:`) и запятыми (`НАПРИМЕР, VLAD`).

- **Обновление пиктограмм для фото и видео файлов в админ-панели (2026-08-31)**:
  * Для изображений установлена пиктограмма фотоаппарата (`Camera`).
  * Для видео установлена пиктограмма видеоплеера со значком воспроизведения (`akar-icons/youtube` style), в сдержанном полупрозрачном сером цвете `#8C8E96`.

- **Унификация карточки «Лидер просмотров» в аналитике (2026-08-31)**:
  * Текст «просмотров» перенесен под большую цифру в формате: `просмотров у <Название проекта>` (например: `18` сверху, `просмотров у CPO-GROUP` снизу).
  * Теперь все 4 карточки аналитики имеют полностью идентичную структуру: крупная цифра сверху и единый поясняющий текст снизу.

- **Геометрия плавающих кнопок в аналитике (2026-08-31)**:
  * Кнопкам `СБРОСИТЬ` (белая) и `ОБНОВИТЬ` (синяя) задано полное закругление со всех сторон (`borderRadius: 56px`), аналогично кнопкам `+` и `СОХРАНИТЬ` на других страницах.
  * Убрана обрезка внешним контейнером, кнопки соприкасаются вплотную (`gap-0`) с органичным наложением скруглений.

- **Навигация сайта: переход к разделу «ВСЕ РАБОТЫ» (2026-08-31)**:
  * В меню навигации кнопка «ПРОЕКТЫ» теперь плавно скроллит к блоку каталога «ВСЕ РАБОТЫ» (`#works`) вместо начальных пяти роликов в шапке.

- **Удаление значка корзины из кнопки «СБРОСИТЬ» в аналитике (2026-08-31)**:
  * Значок корзины убран, кнопка теперь содержит чистый типографический текст `СБРОСИТЬ` в едином стиле с кнопками `ОБНОВИТЬ` и `СОХРАНИТЬ`.

- **Выравнивание и отступы подзаголовков аналитики (2026-08-31)**:
  * Подзаголовки `КЛИКИ ПО КНОПКАМ И КОНВЕРСИЯ В ЛИДЫ` и `РЕЙТИНГ ПРОСМОТРА ВИДЕО` строго выровнены по левой направляющей линии главного заголовка `АНАЛИТИКА`.
  * Добавлен дополнительный верхний отступ `32px` над каждым из этих блоков для гармоничной визуальной иерархии.

- **Безопасная смена PIN-кода и редизайн раздела настроек/редакторов (2026-08-31)**:
  * В карточке «Мой профиль и PIN-код» убран лишний фон и рамка.
  * Реализован защищенный флоу смены пароля/PIN-кода: поле PIN замаскировано, при нажатии на «СМЕНИТЬ PIN» открывается модальное окно с проверкой текущего пароля и подтверждением нового.
  * Блок «Управление редакторами и доступом» строго скрыт от обычных редакторов и доступен только для роли главного разработчика (`role: 'dev'`).
  * У карточек редакторов убраны подложки и лишние границы; все инпуты переведены на единый общесайтовый стиль с рамками `#26282C` и деликатной подсветкой.
  * Кнопки «Добавить редактора» и удаления приведены к общему сдержанному стилю без кислотных синих градиентов.

- **Тонкая настройка скруббера видео и стопкадров (2026-08-31)**:
  * Убран эффект тени у синего ползунка таймлайна (`shadow-none`, плоский строгий стиль).
  * Фон тегов точного шага времени (`-0.1s`, `-1s`, `+0.1s`, `+1s`) сделан светлее (`#2E2F35` с тонким контуром `border-white/10`), чтобы они четко читались и не сливались с темным фоном видео.

- **Редизайн модального окна кадрирования обложек (2026-08-31)**:
  * Размер модального окна увеличен (`max-w-5xl` с широкой рабочей областью canvas до `880x480px`).
  * Убраны внешние скругления окна (`rounded-none`, строгий кинематографичный прямоугольный стиль).
  * В правом верхнем углу добавлена кнопка закрытия `X`.
  * Чипсы выбора пропорций (`16:9 (ГОРИЗОНТ)`, `9:16 (REELS)`, `1:1 (КВАДРАТ)`, `СВОБОДНЫЙ`) переведены на стиль чипсов категорий со страницы проектов (`padding: 8px 16px`, `borderRadius: 56px`, синий акцент).
  * Убраны лишние нижние кнопки сдвигов и зума.
  * Снизу по центру аккуратно расположены две круглые кнопки: `ОТМЕНА` (темно-серая) и `ПРИМЕНИТЬ` (синяя).

- **Удаление обводки у тегов шага времени стопкадра (2026-08-31)**:
  * У тегов точного шага времени (`-0.1s`, `-1s`, `+0.1s`, `+1s`) полностью убрана внешняя обводка (`border-none`), оставив чистый плоский серый фон `#2E2F35` с контрастным текстом.

- **Исправление выделения/копирования текста и перенос подписи позиции слота (2026-08-31)**:
  * Убран блокирующий класс `select-none` с контейнеров админки и секции FAQ — теперь весь текст свободно выделяется курсором и копируется через Cmd+C / контекстное меню.
  * Надпись слота сетки перенесена в левую колонку карточки: расположена прямо под обложкой (между рамкой обложки и кнопками управления).
  * Префикс «Слот сетки:» и тёмная плашка-подложка убраны — текст оформлен чистым аккуратным шрифтом (`ПОЗИЦИЯ #1 (L • 964×542)`).

- **Финальные отступы модалки кадрирования и центрирование подписей позиций (2026-08-31)**:
  * В модальном окне обрезки убраны внешние обводки (как у окна, так и у области кадра: `border-none`).
  * Установлены строгие отступы: сверху и слева/справа у главного заголовка по 24px, между заголовком и чипсами 12px.
  * Между кнопками снизу `ОТМЕНА` и `ПРИМЕНИТЬ` паддинг/отступ установлен в 0 (`gap-0`), сверху и снизу от кнопок добавлены отступы по 24px.
  * Подпись позиции слота (`ПОЗИЦИЯ #...`) выровнена строго по центру превью обложки (`text-center w-full block`).
  * У подсказок рекомендуемого формата видео удалена иконка `info` — текст отображается чистым моноширинным стилем.

- **Строгое разграничение прав: редактирование/добавление/удаление разделов только у разработчика (2026-08-31)**:
  * Блок изменения названия раздела («НАЗВАНИЕ РАЗДЕЛА») и кнопка «УДАЛИТЬ» раздел строго скрыты для роли редактора (`userRole === 'editor'`) и доступны только главному разработчику (`userRole === 'dev'`).
  * Кнопка «+ НОВЫЙ РАЗДЕЛ» и соответствующее модальное окно добавления раздела скрыты для редакторов.
  * Редактор видит только сам список категорий (чипсы) и карточки работ для наполнения и редактирования контента.

- **Верификация доступа: блок управления редакторами скрыт от роли редактора (2026-08-31)**:
  * Блок «УПРАВЛЕНИЕ РЕДАКТОРАМИ И ДОСТУПОМ» в настройках настроен с жестким условием `{userRole === 'dev' && (...)}`.
  * При входе под редактором (`role: 'editor'`) этот блок и блок экспорта/бэкапа базы данных полностью отсутствуют на странице.
  * Редактору доступна только верхняя карточка собственного профиля (смена аватарки, имени и защищенная смена своего PIN-кода).

- **Редизайн шапки видеоплеера и элементов управления VideoModal (2026-08-31)**:
  * У круглых стрелок навигации «Предыдущее / Следующее» в дефолтном состоянии убраны обводки (`border-none`), при наведении сохраняется синий цвет (`hover:bg-[#1458E6]`).
  * Полностью убран всплывающий по центру текст/HUD с переключением роликов (`СЛЕДУЮЩЕЕ (23/33)`), экран плеера теперь чистый.
  * Верхняя плашка с названием ролика и номером плейлиста переверстана в стиле снекбара сайта (Frame 220): белый прямоугольный фон, синий бейдж с номером ролика `[23/32]` и четкий черный текст названия в верхнем регистре.

- **Удаление всех иконок info у всех полей ввода и подсказок (2026-08-31)**:
  * Полностью удалены синие иконки `info` (`<Info ... />`) у всех инпутов загрузки видео и форматов во всех разделах админки (Главная/Reels, Проекты/Works, Клиенты/Clients).
  * Текст подсказок о рекомендуемых форматах теперь везде оформлен чистым моноширинным текстом без лишних визуальных акцентов.

- **Редизайн модального окна добавления работы в Hero ленту (2026-08-31)**:
  * Убраны внешние обводки формы модального окна, установлен фирменный фон `#141416` с глубокой тенью и острыми углами (`rounded-none`).
  * Карточка добавляемого проекта очищена от лишних рамок и фоновых подложек: отображается чистое превью обложки, заголовок на русском и английском языках.
  * Исправлено отображение обложки с безопасным `onError` фолбэком.
  * Убраны перегруженные карточки с размерами и пикселями — выбор позиции заменен на аккуратные моноширинные чипсы-кнопки (`ПОЗИЦИЯ #1`, `ПОЗИЦИЯ #2`, ... `В КОНЕЦ (#N)`).
  * Снизу модалки добавлена центрированная кнопка «ОТМЕНА» в едином стиле.

- **Стандартизация плавающего квадратного крестика закрытия во всех модальных окнах (2026-08-31)**:
  * Во всех модальных окнах (`ImageCropModal`, `VideoModal`, «Смена PIN-кода», «Новый раздел», «Добавить в Hero», «Подтверждение/Удаление») крестик закрытия вынесен в плавающую квадратную кнопку (`40x40px`) снаружи окна (`top-0 left-[calc(100%+4px)]`).
  * Кнопка выполнена на фирменном темном фоне `#141416`, с эффектом инверсии при наведении (белый фон, черный крестик).

- **Закрытие всех модальных окон при клике по фону (overlay) и клавише Escape (2026-08-31)**:
  * Для всех модальных окон (`ImageCropModal`, `VideoModal`, `isPinModalOpen`, `isNewCategoryModalOpen`, `addToHeroModalItem`, `confirmModal`) настроено закрытие при клике на пустое пространство фона (`backdrop`), а клики внутри самого окна защищены через `e.stopPropagation()`.
  * Также добавлена поддержка закрытия по клавише `Escape`.

- **Фиксация дизайн-системы модальных окон и форм (2026-08-31)**:
  * Единый стандарт формы: фон `#141416`, строгие прямые углы (`rounded-none`), отсутствие внешних обводок (`border-none`), глубокая тень `shadow-2xl`, внутренние отступы `36px 36px 32px 36px`.
  * Заголовок: моноширинный шрифт 20px, uppercase, 700, белый цвет.
  * Поля ввода (Input): высота 40px, фон прозрачный с подсветкой при фокусе, рамка `#26282C`, текст 15-16px моно bold.
  * Кнопки действий: пилюли `rounded-full` / `56px`, `padding: 8px 16px`, `gap: 0`, моно bold uppercase 16px.
  * Кнопка закрытия: плавающий квадрат `40x40px` снаружи справа сверху (`left-[calc(100%+4px)] top-0`) с инверсией цвета.
  * Закрытие по клику на фон (overlay backdrop) и по клавише `Escape`.

- **Обновление блока «Управление редакторами и доступом» (2026-08-31)**:
  * Убраны разделительные линии между строками пользователей (`border-none`).
  * Добавлена возможность создания как Редакторов (`+ Редактор`), так и Разработчиков (`+ Разработчик`).
  * Добавлено интерактивное переключение роли (клик по пилюле `Разработчик` / `Редактор` меняет права пользователя).
  * Выровнена ширина всех полей ввода (сетка `grid-cols-3` с одинаковой шириной для «Отображаемое имя», «Логин для входа», «PIN-код / пароль»).
  * Добавлена проверка уникальности логина (при дублировании логина поле подсвечивается с сообщением «занят»).
  * Добавлены правила и валидация для PIN-кода (минимум 4 символа с предупреждением при нарушении).

- **Восстановление отображения обложки в окне «Добавить в Hero» (2026-08-31)**:
  * Добавлена универсальная поддержка всех форматов медиа (видеообложки с `#t=`, стандартные изображения, fallback на `video_url` и превью) в окне выбора позиции ролика в Hero.
  * Устранен сбой загрузки обложек с видеофайлами через `<img>`.

- **Исправление поля «Аватарка профиля» (2026-08-31)**:
  * Поле растянуто на всю доступную ширину строки (`w-full` вместо `max-w-md`).
  * Плейхолдер сокращен до «Загрузите фото или перетащите файл», что исключает обрезание текста кнопкой прикрепления.

- **Обновление позиционирования и отступов снекбара в VideoModal (2026-08-31)**:
  * Белая плашка с номером и названием ролика прижата вплотную к левому верхнему краю контейнера плеера (`top: 0, left: 0`).
  * Внутренние отступы белой плашки установлены в `12px` (`padding: 12px`).
  * Внутренние отступы синего тега с номером `2/33` установлены в `8px` (`padding: 8px`).

- **Редизайн карточек «Управление доступами» и генераторы реквизитов (2026-08-31)**:
  * Каждый пользователь оформлен в виде отдельной карточки (`bg-[#141416] rounded-[24px]`).
  * Увеличен размер круга аватарки (64x64px), цвет: синий для разработчиков (`#1458E6`), серый для редакторов (`#232326`).
  * Тег роли всегда выполнен в сером стиле (`#232326`), при клике интерактивно переключает роль.
  * Поля ввода (Имя, Логин, Пароль) имеют строго фиксированную равную ширину (`grid-cols-3`).
  * В поле Логина добавлена кнопка генерации логина (исходя из имени) и подсказка правил.
  * В поле Пароля добавлена кнопка показать/скрыть (Eye/EyeOff), кнопка генерации безопасного PIN-кода и подсказка «от 4 знаков».
  * При создании нового пользователя создается чистая пустая строка.
  * Кнопка добавления вынесена в плавающую панель снизу (большой белый кругляш с плюсиком рядом с кнопкой «СОХРАНИТЬ»).

- **Корректировка паддингов в снекбаре VideoModal (2026-08-31)**:
  * Верхние и нижние паддинги белой плашки уменьшены на 4px (с 12px до 8px).
  * Верхние и нижние паддинги синего тега номера ролика уменьшены на 4px (с 8px до 4px), что сделало плашку более компактной и аккуратной.

- **Стандартизация иконок удаления в блоке F.A.Q. (2026-08-31)**:
  * Убрана текстовая кнопка «УДАЛИТЬ» и синяя иконка HelpCircle из шапки карточки F.A.Q.
  * Установлена стандартная аккуратная иконка корзины `Trash2`, полностью соответствующая общему стилю админки.

- **Обновление модального окна «Добавить в Hero» (2026-08-31)**:
  * Блок с обложкой и названием проекта выделен на отдельной стильной темной плашке (`bg-[#1C1D24] rounded-[16px]`).
  * Кнопки выбора позиции сокращены до лаконичных номеров: `#1`, `#2`, `#3`, `#4`, `#5`, `В КОНЕЦ (#N)`.

- **Редизайн модалки «ДОБАВИТЬ НА ГЛАВНУЮ В HERO» и интерактивный снекбар (2026-08-31)**:
  * Заголовок модального окна обновлен на «ДОБАВИТЬ НА ГЛАВНУЮ В HERO».
  * Увеличен размер превью обложки (в 2.5 раза), убраны фоновая плашка и рамка у карточки проекта.
  * Последний тег позиции теперь отображает `#N (NEW)`.
  * Реализован выбор позиции кликом по чипсам с синей подсветкой активного слота.
  * Добавлены кнопки «ОТМЕНА» и «СОХРАНИТЬ».
  * После сохранения в верхнем снекбаре появляется интерактивная кнопка-ссылка «ПЕРЕЙТИ», позволяющая мгновенно перейти к добавленному ролику в разделе Hero.

- **Стилизация карточек пользователей и отступы заголовков (2026-08-31)**:
  * Заголовкам подразделов в «Аналитике» и «Управлении доступами» задан отступ слева `padding-left: 24px` для идеального выравнивания по сетке.
  * У всех аватарок пользователей убрана обводка — оставлена чистая заливка.
  * Карточкам пользователей задан внутренний отступ `20px` со всех сторон.
  * Кнопка показа/скрытия пароля 👁 прижата к правому краю с заливкой фона (`#232326`), кнопка генератора ✨ расположена рядом с ней.
  * Блок роли и кнопки удаления зафиксирован строго на ширине `172px` (`172×36px`), за счет чего ширина всех 3 полей ввода одинакова в каждой строке независимо от роли пользователя.

- **Плавающее меню добавления пользователей и финальная полировка Настроек (2026-08-31)**:
  * По клику на белый плюсик внизу вкладки «Настройки» теперь всплывают две плашки выбора: «+ РАЗРАБОТЧИК» (синяя) и «+ РЕДАКТОР» (темная).
  * У карточек пользователей полностью убраны обводки — только мягкий фон `#141416` и скругление `24px`.
  * Блок роли и иконки корзины (`172px`) выровнен по правому краю (`justify-end`) и прижат к нижней линии полей ввода (`items-end`).
  * Заголовку «УПРАВЛЕНИЕ ДОСТУПАМИ» добавлен верхний отступ `paddingTop: 32px`.
  * Заголовок «РЕЗЕРВНОЕ КОПИРОВАНИЕ И ЭКСПОРТ БАЗЫ» вынесен за пределы карточки, внутри карточки размещен пояснительный текст и кнопки экспорта/импорта.

- **Исключение дублирования текущего пользователя в «Управлении доступами» (2026-08-31)**:
  * В блоке «УПРАВЛЕНИЕ ДОСТУПАМИ» теперь отображаются только *другие* пользователи (редакторы и разработчики).
  * Текущий авторизованный пользователь видит свои данные исключительно в карточке профиля сверху и не дублируется в общем списке.
  * Редакторы по-прежнему видят только свою строку профиля (без доступа к управлению другими аккаунтами и экспорту).

- **Прямоугольные плашки добавления ролей по ховеру (2026-08-31)**:
  * Убраны круглые пиллы («колбасы»).
  * Сделаны аккуратные прямоугольные белые плашки (`rounded-none`, `bg-white text-black hover:bg-[#1458E6] hover:text-white`), появляющиеся точно по ховеру над кнопкой плюсика внизу экрана.
  * Текст приведен к лаконичному виду без знака плюс: «РАЗРАБОТЧИК» и «РЕДАКТОР», полностью повторяя поведение и геометрию главного навигационного меню сайта.

- **Ховер иконок генераторов и подсказки (?) для логина и пароля (2026-08-31)**:
  * Для кнопок генераторов ✨ убраны любые фоновые заливки: по дефолту иконка серая, при ховере плавно становится чисто белой без фонового прямоугольника.
  * Текстовые подсказки «латиница» и «от 4 знаков» заменены на лаконичный значок `(?)` рядом с названиями полей («логин» и «пароль»), отображающий подробную подсказку при наведении.
  * У аватарки в блоке профиля также снята обводка.

- **Уменьшение отступов между однотипными карточками до 4px (2026-08-31)**:
  * В «Аналитике» для строк «Клики по кнопкам» и «Рейтинг просмотра видео» отступ между элементами списка уменьшен до `4px` (`gap-[4px]`).
  * В «Управлении доступами» отступ между карточками пользователей также приведен к `4px` (`gap-[4px]`), визуально объединяя их в цельную группу.

- **Редизайн карточки профиля в единую компактную строку (2026-08-31)**:
  * Карточка «МОЙ ПРОФИЛЬ» приведена к единому гармоничному виду нижних карточек: в одну горизонтальную строку с паддингом `20px` и скруглением `24px`.
  * Убран лишний read-only инпут с точками и громоздкая зона загрузки фото.
  * Аватарка профиля меняется прямо в один клик по аватарке (с иконкой камеры при ховере).
  * 3 колонки сетки: `имя`, `логин` (с кнопкой автогенерации и подсказкой `(?)`) и стильная кнопка `СМЕНИТЬ PIN` для открытия модального окна безопасной смены пароля.
  * Правый блок роли выровнен по общей сетке `172px`.

- **Верстка карточки профиля по скриншоту из Figma (2026-08-31)**:
  * Слева расположена круглая аватарка пользователя (`72×72px`).
  * Справа на первой строке — длинный стандартный инпут добавления аватарки `DropFileInput` (как у обложек проектов), а в правом верхнем углу — тег роли (`РАЗРАБОТЧИК` / `РЕДАКТОР`).
  * На второй строке рядом идут поля `имя` и `логин` (с кнопкой генерации ✨ и подсказкой `(?)`).
  * Снизу аккуратная белая кнопка `СМЕНИТЬ ПАРОЛЬ` без лишних иконок, открывающая модальное окно смены PIN-кода.

- **Перенос иконок подсказок `(?)` внутрь инпутов с интерактивным тултипом (2026-08-31)**:
  * Иконки вопросов убраны из текстовых заголовков и перенесены в правый блок кнопок внутри инпутов (вместе со звёздочками и глазиком).
  * При наведении на иконку вопроса `(?)` плавно всплывает красивое контрастное окно-подсказка с правилами ввода.

- **Идеальное выравнивание полей профиля по сетке (2026-08-31)**:
  * Заголовок «МОЙ ПРОФИЛЬ» полностью убран.
  * Правая граница инпута «аватарка» и полей «имя» / «логин» теперь строго заканчивается по единой вертикальной направляющей линии (перед фиксированным правым блоком `172px`).
  * Полное визуальное единообразие всех карточек пользователей на странице.

- **Интерактивные подсказки `?` в стиле Instagram-тултипа (2026-08-31)**:
  * В правой части строки над инпутом размещен аккуратный круглый серый значок `?`.
  * При наведении на значок всплывает точная посимвольная блочная подсказка в фирменном стиле (фон `#3A3A3A`, цвет `#E6E6E6`, шрифт Geist Mono, капс, построчное облегание как при наведении на Instagram в контактах).

- **Обновление бейджей ролей и интерактивный селектор роли (2026-08-31)**:
  * В верхней карточке профиля текущего пользователя бейдж роли стал более компактным и аккуратным (`28px`, шрифт `11px`, акцентный синий цвет `#1458E6` для разработчика).
  * В карточках других пользователей («Управление доступами») вместо циклического переключения сделан полноценный выпадающий список (Dropdown) со стрелочкой вниз `ChevronDown`, позволяющий мгновенно выбирать и менять роль на `Разработчик` или `Редактор` (с возможностью легкого расширения новыми ролями в будущем).

- **Добавление дополнительных отступов для кнопки «Сменить пароль» (2026-08-31)**:
  * Для кнопки «Сменить пароль» добавлены паддинги `+12px` сверху и снизу (`pt-[12px] pb-[12px]`), что дает больше воздуха в нижней части карточки профиля.

- **Единая терминология «Пароль» и подсказка в модальном окне (2026-08-31)**:
  * Во всем модальном окне смены пароля термин `PIN-код` заменен на `Пароль` (заголовок «СМЕНА ПАРОЛЯ», поля «текущий пароль», «новый пароль», «повторите новый пароль», сообщения валидации).
  * У поля «новый пароль» добавлен круглый значок `?` с фирменным тултипом в стиле Instagram при наведении.

- **Увеличение отступов кнопки «Сменить пароль» до 24px (2026-08-31)**:
  * Для контейнера кнопки «Сменить пароль» заданы прямые инлайн-стили `style={{ paddingTop: '24px', paddingBottom: '24px' }}` для гарантированного и четко видимого отступа.

- **Перенос текста описания бэкапа в 2 строки (2026-08-31)**:
  * Текст описания в блоке «Резервное копирование и экспорт базы» разделен на две сбалансированные строки для улучшения читаемости.

- **Корректировка отступов кнопки «Сменить пароль» на 12px (2026-08-31)**:
  * Для контейнера кнопки «Сменить пароль» установлен аккуратный инлайн-стиль `style={{ paddingTop: '12px', paddingBottom: '12px' }}`.

- **Стилизация выпадающего списка ролей без скруглений и обводок с паддингами 20px (2026-08-31)**:
  * У выпадающего списка ролей убраны скругления (`rounded-none`) и обводки (`border-none`).
  * Добавлены отступы `20px` со всех сторон (`style={{ padding: '20px' }}`), элементы списка выполнены в виде строгих прямоугольных плашек.

- **Устранение синей рамки аватара и функционал обрезки аватарок (2026-08-31)**:
  * В сайдбаре у аватара убрана синяя подложка и рамка при загруженном фото (`border-none`, чистая заливка).
  * Для аватарок в настройках и в списке пользователей реализован оверлей с кнопкой «ОБРЕЗАТЬ»: при наведении/клике на круглую фотографию на всю ширину кружка плавно всплывает синяя кнопка обрезки, открывающая стандартное модальное окно кадрирования `ImageCropModal`.

- **Финальная доработка выпадающего меню ролей по скриншоту (2026-08-31)**:
  * Убран внешний контейнер/фон и лишние отступы.
  * Прямоугольные плашки меню склеены встык с нулевым отступом (`gap-0`).
  * Текстовая галочка заменена на четкую векторную иконку `<Check />` из Lucide.

- **Специализированный режим кадрирования аватарок 1:1 (2026-08-31)**:
  * В модальном окне обрезки для аватарок заголовок изменен на «ОБРЕЗАТЬ АВАТАРКУ».
  * Верхние чипсы пропорций скрыты (`hidePills={true}`).
  * Обрезка жестко зафиксирована в строгом соотношении 1:1 (квадрат), что идеально подходит под круглый формат аватаров.

- **Комплексное исправление ошибок по аудиту сайта (2026-08-31 / 2026-09-01)**:
  1. *Очистка дублей тяжелых файлов*: Сконвертирован тяжелый HerzenRowing (546 МБ -> 45.6 МБ MP4 faststart), удалены 3 дубликата .mov файлов — освобождено 1.63 ГБ места.
  2. *API Content & Upload*: Добавлена строгая типизация `StoredContentData`, валидация структуры входных данных, убраны `any`.
  3. *Аналитика*: Внедрена нормализация названий видео (регистронезависимый подсчет) и защита от дублирования ключей.
  4. *DropFileInput*: Убран фейковый расчет веса файла через хэш, добавлено честное отображение типа медиа (`MP4 • ВИДЕО` / `JPG • ФОТО`).
  5. *Плавный скролл (Lenis)*: Исправлена рекурсивная утечка `requestAnimationFrame` при демонтировании компонента.
  6. *Троттлинг запросов*: Запросы обновления контента по `window.focus` ограничены 15 секундами.
  7. *ProcessSection*: Добавлена CSS-изоляция контекста наложения (`isolate`).
  8. *FAQ*: Реализована плавная анимация аккордеона через `AnimatePresence` / `motion.div` без рывков контента.
  9. *ContactSection Tooltip*: Добавлено адаптивное позиционирование тултипа Instagram Meta Notice для предотвращения вылета за экран.
  10. *WorksSection*: Заменен нестандартный `scale-108` на валидный Tailwind класс `scale-105`.
  11. *ReelsSection*: Контейнер переведен на адаптивный `w-full max-w-[964px]`.
  12. *Кнопки навигации*: Всем кнопкам бургер-меню добавлен явный атрибут `type="button"`.
  13. *SEO & OpenGraph*: Добавлен экспорт `viewport`, `openGraph.images` и `twitter.images` в `layout.tsx`.
  14. *Preloader*: Добавлен флаг `sessionStorage`, предотвращающий повторный принудительный показ прелоадера в рамках одной активной сессии.
  15. *Favicon*: Создан `favicon-light.png` в `public/` для устранения 404 ошибки на светлой теме.

- **Откат аккордеона FAQ и исправление секундной ошибки постера видео (2026-09-01)**:
  * FAQ возвращен к мгновенному чистому переключению по дизайну Figma (убрана анимация высоты `motion.div`).
  * В `ReelsSection` устранена ошибка загрузки постера: ранее при указании видео-файла (`.mp4#t=12.9`) в качестве обложки тег Next.js `<Image />` пытался загрузить MP4 как картинку, вызывая секундный сбой `<img>`. Теперь компонент проверяет тип медиа и рендерит `video` с кадром превью без ошибок.

- **Рефакторинг кодовой базы (2026-09-01)**:
  * Создан модуль общих утилит `lib/media.ts` (`isVideoMedia`, `cleanMediaUrl`, `extractTimecode`), устранены дублирования детекции типов медиа в `WorksSection`, `ReelsSection`, `DropFileInput`.
  * Вынесены модульные компоненты админ-панели в `app/admin/components/`:
    - `AdminPinModal.tsx`: модальное окно смены пароля с валидацией и тултипом;
    - `AdminConfirmModal.tsx`: диалог подтверждения опасных и общих действий;
    - `AdminToast.tsx`: компонент анимации всплывающих уведомлений.
  * Улучшена читаемость и поддержка кода, снижен размер монолитного файла `app/admin/page.tsx`.
  * Все TypeScript-типы строго соблюдены, `npm run build` завершен с кодом 0.

- **Адаптация под мобильные устройства (2026-09-01)**:
  * Прелоадер на мобильных экранах (< 768px) отключен — контент загружается и открывается мгновенно.
  * В секции услуг (`ProcessSection`) создан отдельный мобильный слой (`block md:hidden`):
    - Карточки растянуты на 100% ширины экрана, расположены друг под другом с `gap-0`;
    - Анимация скролла и залипание на 400vh на мобильных устройствах отключены;
    - Все 4 заголовка карточек оформлены единым одинаковым кеглем (`clamp(38px, 11vw, 54px)`).
    - Десктопная версия осталась на 100% нетронутой и зафиксированной.

- **Деплой на Vercel (2026-09-01)**:
  * Все изменения (мобильная версия услуг, отключение мобильного прелоадера, аудит-фиксы и модульный рефакторинг) закомичены и запушены в ветку `main` GitHub-репозитория (`https://github.com/alevtinaksen/electricrate-site.git`).
  * Запущен автоматический деплой на Vercel по адресу: `https://electricrate-site.vercel.app/`.

- **Синхронизация ассетов и повторный пуш на Vercel (2026-09-01)**:
  * В `.gitignore` скорректировано правило для `public/uploads`: теперь все постеры, аватарки и статические изображения (`.jpg`, `.png`, `.webp`, 38 МБ) отслеживаются в Git и передаются на Vercel при сборке.
  * Все обложки и постеры закомичены и запушены в `main` (коммит `4c867f3`).
  * Vercel пересобирает и обновляет сайт на `https://electricrate-site.vercel.app/`.

- **Исправление верстки на широких экранах >1500px (2026-09-01)**:
  * Ранее правый блок был жестко прижат к правому краю, а левый сайдбар имел фиксированную ширину 538px, из-за чего на экранах от 1500px до 4K между ними возникала пустая черная дыра (400-1000px).
  * Решение: левый сайдбар (`Sidebar`) переведен в `md:flex-1` (заполняет всё пространство слева), а правый блок ленты зафиксирован ровно в 964px. Сайдбар вплотную стыкуется с лентой проектов на любых разрешениях экрана без единого пустого пикселя.
  * Кнопка меню («СВЯЗАТЬСЯ») динамически спозиционирована точно на стыке двух колонок (`calc(100vw - 964px + 20px)`).
  * Изменения закомичены в `main` (коммит `3be5218`) и отправлены на Vercel.

- **Исправление ширины сайдбара и стыковки колонок (2026-09-01)**:
  * Возвращены стабильные адаптивные размеры сайдбара (`360px` / `440px` / `538px`), что полностью исключает обрезание текста «ВЛАД САПУНОВ» и контактов на ноутбуках и узких десктопах.
  * Правая колонка с лентой проектов плотно пристыкована слева непосредственно к сайдбару (`md:items-start` + `md:ml-0`), исключая появление щелей/зазоров между блоками.
  * Плавающая кнопка («СВЯЗАТЬСЯ») аккуратно спозиционирована в начале ленты проектов (`md:left-[380px]` / `lg:left-[460px]` / `xl:left-[558px]`).
  * Коммит `0bb64b6` запушен в `main` на Vercel.

- **Идеальная верстка для ультрашироких экранов 1920px+ (2026-09-01)**:
  * Левый сайдбар растягивается (`md:flex-1`), заполняя всё пространство слева от 964px.
  * Правая лента проектов фиксирована в 964px и прижата вправо без пустот.
  * Размер текста «ВЛАД САПУНОВ» скорректирован (`clamp(32px, 3.8vw, 84px)`), исключая обрезание на любых ширинах сайдбара.
  * Плавающая кнопка («СВЯЗАТЬСЯ») привязана к стыку колонок (`calc(100vw - 964px + 20px)`).
  * Коммит `ea12ab7` запушен на Vercel.

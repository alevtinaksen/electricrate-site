# 2026-08-29 — Лог разработки

## Влад Сапунов — Сайт-портфолио и админ-панель

### Выполненные задачи:
1. **Секция «Клиенты»**:
   - Ховер изолирован строго на тексте названия клиента (логотип 54×54 и точка `·` не окрашиваются и не входят в ховер-плашку).
   - Первый клиент объединен в единое название: «Петербургский нефтяной терминал» (выделяется целиком).
2. **Секция «Обо мне»**:
   - Фотография Влада растянута на всю ширину правой части блока.
3. **Аккордеон FAQ**:
   - Добавлены отступы `padding: 8px` слева и справа в строку вопроса и раскрывающийся контент, исключено прилипание текста и стрелочки к краям белого фона при ховере/открытии.
4. **Админ-панель (`/admin`)**:
   - **Раздел «Услуги»**: убрана подчеркивающая линия и лишний текст. Заголовки карточек приведены к виду: `Карточка №1 (синяя)`, `Карточка №2 (белая)`, `Карточка №3 (черная)`, `Карточка №4 (синяя)`.
   - **Раздел «Обо мне»**: убраны подчеркивающие линии в верхнем и нижнем блоках. Текстареи увеличены (`rows={8}`), текст теперь помещается целиком без внутреннего скролла.
   - **Раздел «Клиенты»**: убраны подчеркивающие линии под блоками карточек.
   - **Раздел «FAQ»**: удалена верхняя кнопка «Добавить вопрос», добавление перенесено на нижний плюс. Новые вопросы и проекты теперь всегда добавляются на самый верх списка.
   - **Плавающие кнопки (+ и СОХРАНИТЬ)**: соединены вместе без отступа (`gap-0`). Настроен интерактивный ховер: белый плюсик при наведении становится синим с белой иконкой; синяя кнопка «СОХРАНИТЬ» при наведении становится белой с черным текстом.
   - **Псевдоотступ снизу**: добавлен во всех вкладках админки (`h-[100px]`), чтобы плавающий блок кнопок не закрывал нижний контент при скролле.
   - **Левый сайдбар**: установлены маргины по 12px сверху, снизу и слева.

- Добавлена и настроена заглушка `public/placeholder.png` для всех разделов админки: если фото отсутствует или не смогло загрузиться (`onError`), автоматически подставляется переданная иконка-заглушка.

- **FAQ аккордеон и типографика**:
  * Заголовок `F.A.Q.` увеличен до **254px** (`fontSize: clamp(96px, 20vw, 254px)`, `letter-spacing: -5.08px`, `line-height: 90%`) в точном соответствии с макетом из Figma.
  * Раскрытый аккордеон оформлен в виде сплошной белой плашки (`bg-white text-black`) с точными отступами: `padding: 4px 8px 30px 8px`, `gap: 8px` между вопросом и 2-колоночным черным текстом ответа.
- **Бургер-меню в сайдбаре**:
  * Ширина каждой плашки пункта меню теперь динамически подстраивается под длину своего текста (`w-fit inline-block`, `items-start`).
  * Внутри каждой плашки установлены горизонтальные отступы `px-[8px] py-[4px]`.
  * В самом верху списка пунктов меню добавлен переключатель языка (`EN` / `RU`).

- **Клиенты**:
  * «Петербургский нефтяной терминал» теперь переносится на две строки (`ПЕТЕРБУРГСКИЙ` / `НЕФТЯНОЙ ТЕРМИНАЛ`), гарантируя, что длинное название идеально помещается в правый контейнер и не заходит на левый сайдбар или за пределы экрана. Ховер охватывает обе строки единым белым блоком, логотип и точка остаются снаружи.
- **FAQ аккордеон**:
  * Стрелочка `▲` / `▼` теперь выровнена по верхнему краю (`items-start`), поэтому при переносе длинных вопросов на 2+ строки стрелка всегда прижата к верхнему правому краю.
  * Падинги установлены по 8px (`padding: 8px 8px 30px 8px` в открытом состоянии и `py-[8px] px-[8px]` в закрытом).
- **Социальные кнопки в нижнем углу сайдбара**:
  * Убрана кнопка `VK`.
  * Иконка/текст почты заменен на значок собаки `@` (остались 2 круглые кнопки: `TG` и `@`).

- **Резервная копия (Бэкап)**:
  * Создан стабильный коммит `5cc54ad`, тег `backup-stable-state`, ветка `backup-stable-state` и отправлены на GitHub.
  * Создан локальный архив `.backups/backup-stable-state.zip`.
  * В любой момент можно мгновенно откатиться к этой точке командой `git reset --hard backup-stable-state`.

- **Редизайн главного экрана и сайдбара**:
  * **Фон левого сайдбара**: установлен более светлый оттенок `#141416` (на контрасте с черным фоном правого контента `#0D0D0D`).
  * **Переключатель RU/EN**: расположен в верхнем левом углу сайдбара (активный — `#2957DE` с белым текстом, неактивный — белый с черным текстом).
  * **Имя «ВЛАД САПУНОВ»**: выровнено строго по правому краю сайдбара (`text-right`, `items-end`, `fontSize: clamp(52px, 6.8vw, 128.49px)`).
  * **Центральный кружок 156×156**: добавлен анимированный слайдшоу кадров со съемок с плавной кроссфейд-анимацией каждые 3 секунды.
  * **Текст в нижнем левом углу**: `ВИДЕОМЕЙКЕР ПОЛНОГО ЦИКЛА. КАРТИНКА УРОВНЯ КИНО — ОТ ИДЕИ ДО МАСТЕРИНГА` (`Geist Mono`, 20px, 700, line-height 110%, max-w 426px).
  * **Контакты (`TG` и `EM`)**: перенесены в правую часть экрана, зафиксированы по центру по вертикали.
  * **Нижняя панель управления**: плавающий блок по центру внизу объединяет белую круглую кнопку бургер-меню `(=)` (со списком разделов при открытии) и синюю кнопку `СВЯЗАТЬСЯ` (`bg-[#2957DE]`).

- **Админ-панель (`/admin`)**:
  * Карточка «Деплой и GitHub Репозиторий» убрана из настроек.
  * Тестовый плеер по кнопке «Тест» (в Hero Reels и Портфолио) заменен на единый актуальный компонент `VideoModal`, идентичный видеопопапу с главной страницы.

- **Визуальные настройки**:
  * PNG маски свечения временно скрыты в `AmbientGlowOverlay`.
  * У левой части на сайте убраны закругления (`rounded-none`, прямые углы на всю высоту `100vh` экрана от верхнего до нижнего края).

- **Услуги (Process Section)**:
  * Расположение карточек обновлено:
    - Белая карточка («МОНТАЖ И ЦВЕТ») выровнена строго по центру правой части.
    - Черный блок («ОТ ИДЕИ ДО МАСТЕРИНГА ВЕДУ САМ») прижат к правой стороне.
- **Плавающая панель (Меню + Связаться)**:
  * Перенесена в крайний левый угол правой области контента.
- **Кружок с кадрами со съемок в сайдбаре**:
  * Смена фотографий ускорена до 1.2 с с быстрой динамичной анимацией (0.35 с).
  * В админ-панель (раздел «Обо мне») добавлен блок управления кадрами со съемок: добавление через прямую ссылку или загрузку файлов с компьютера, удаление, просмотр превью.
- **Переключатель RU / EN**:
  * Внутренние горизонтальные падинги кнопок увеличены до 20px (`px-[20px]`).

- **Блок «Обо мне / Сайдбар»**:
  * Падинги текста в левой нижней части установлены ровно по **24px** (`padding: 24px` на контейнер сайдбара).
  * Внутренние падинги кнопок `RU` и `EN` установлены по **20px**.
  * Смена фотографий в центральном кружочке работает быстро (1.2 с) и динамично.
  * В админке добавлен блок добавления и удаления кадров со съемок.
- **Услуги (Process Section)**:
  * Белая карточка отцентрирована, черная прижата к правому краю.
- **Кнопки управления**:
  * Кнопка бургер-меню и кнопка «СВЯЗАТЬСЯ» прижаты к левому краю правой области контента.

- **Имя «ВЛАД САПУНОВ» в сайдбаре**:
  * Размер шрифта увеличен (`fontSize: clamp(62px, 9.8vw, 142px)`, `lineHeight: 88%`, `letterSpacing: -2px`), чтобы на любом разрешении надпись занимала практически всю ширину левой части сайта.
  * Установлены отступы от правого верхнего угла: сверху 20px, справа 20px.
- **Лоадер (Preloader)**:
  * Все цифры счетчика процентов и текст лоадера теперь явно используют фирменный моноширинный шрифт `Geist Mono` (`var(--font-geist-mono)`).

- **Исправление сайдбара и типографики**:
  * Переключатель языка `RU` / `EN` прижат встык к верхнему левому углу с отступами **0px** (`top-0 left-0`).
  * Для заголовка «ВЛАД САПУНОВ» заданы отступы: **сверху 20px, справа 20px**.
  * Размер шрифта динамически подстроен через container queries (`clamp(56px, 21.5cqw, 118px)`), благодаря чему слово «САПУНОВ» занимает максимальную ширину и не обрезается на любых разрешениях экрана.

- **Сайдбар и типографика**:
  * Имя «ВЛАД САПУНОВ» занимает максимальную ширину левой части (`clamp(62px, 23.5cqw, 126px)`), от правого края задан отступ **12px**, сверху **20px**.
  * Нижний текст («ВИДЕОМЕЙКЕР ПОЛНОГО ЦИКЛА...») получил отступы слева и снизу по **20px** (`pl-[20px] pb-[20px]`).
- **Плавающая панель**:
  * Кнопки меню и «СВЯЗАТЬСЯ» теперь отступают от левого сайдбара ровно на **20px** (`pl-[20px]`).
  * Меню бургер теперь открывается **по ховеру** (`onMouseEnter` / `onMouseLeave`), а также по клику.
  * Кнопка «СВЯЗАТЬСЯ» по ховеру плавно перекрашивается в **белый цвет с темным текстом** (`hover:bg-white hover:text-[#0B0B0B]`).

- **Синхронизация блока клиентов с админкой**:
  * В `app/page.tsx` подключена передача `clients` (состояние загружается из `/api/content` и локального хранилища).
  * `ClientsSection` переписан на полную динамическую отрисовку: любые изменения названий (RU / EN), логотипов и видео клиентов из админки мгновенно отображаются на сайте.

- **Pixel-Perfect верстка первого экрана (Hero Section)**:
  * **Заголовок («ВЛАД САПУНОВ»)**: строго по Frame 195 (`text-[clamp(64px,7.5vw,120px)] font-bold tracking-[-0.04em] leading-[0.85] text-white uppercase break-words w-full text-right select-none`).
  * **Подзаголовок (Frame D94CEED3)**: `pl-[20px] mb-[24px]` со шрифтом Geist Mono 15-16px leading-snug.
  * **Нижняя интерактивная панель (Frame 135BBEFD)**: отступ слева 20px, отступ снизу ровно 16px (`bottom-[16px]`), `flex items-center gap-0`, синяя пилюля `#1458E6` со скруглением `rounded-full` (по ховеру становится белой).
  * **Языковой переключатель (RU / EN)**: `absolute top-0 left-0 flex` (RU синий `#1458E6`, EN белый `#FFFFFF`).

- **Точные отступы и типографика по DevTools**:
  * Имя «ВЛАД САПУНОВ»: отступы `padding: 12px 20px 0px 0px` (сверху 12, справа 20, снизу 0, слева 0), начертание шрифта возвращено к исходному гармоничному `font-semibold` (`fontWeight: 600`).
  * Нижний подзаголовок («ВИДЕОМЕЙКЕР ПОЛНОГО ЦИКЛА...»): точные отступы `padding: 0px 0px 24px 24px` (снизу 24, слева 24).
  * Нижняя панель («Меню + Связаться»): точный внутренний отступ `padding: 0px 0px 0px 20px` (слева 20).

- **Стек карточек «Услуги» (ProcessSection)**:
  * **Белая карточка («МОНТАЖ И ЦВЕТ»)**: выровнена так, что ее левый край останавливается точно по границе слова «СЪЕМКА» из верхней синей карточки (`left-[130px] sm:left-[165px]`), оставляя буквы `СЪ` видимыми слева.
  * **Черная карточка («ПОЛНЫЙ»)**: прижата к правому краю (`right-0 top-[100px]`), при этом нижняя синяя карточка («КОМАНДА») перекрывает нижнюю часть, оставляя видимым ровно слово `ПОЛНЫЙ`.

- **Исправление сохранения и отображения клиентов**:
  * Удалены любые статические переопределения названия первого клиента: теперь компонент отображает ровно то, что введено в поле «название клиента» в админке (`getClientName(c0)`).
  * Изменения из админки мгновенно сохраняются в базу/файл `content.json` и локальный кэш браузера при нажатии «СОХРАНИТЬ ИЗМЕНЕНИЯ».

- **Стек карточек «Услуги» (ProcessSection)**:
  * Белая карточка («МОНТАЖ И ЦВЕТ») и черная карточка («ПОЛНЫЙ») опущены ровно на 50px ниже (`top-[110px] sm:top-[130px]` и `top-[150px] sm:top-[170px]`).

- **Обновления настроек и блока «Обо мне»**:
  * **Блок «Обо мне» (AboutSection)**: фотография теперь занимает 100% ширины и высоты правой части (`absolute inset-0 w-full h-full object-cover`), автоматически растягиваясь на любую ширину контейнера (500px, 964px, 1200px и т.д.).
  * **Плавающие кнопки справа**: полностью удалены (кнопки TG и EM убраны с правого края).
  * **Настройки админки**: удалено поле VK, кнопка «СВЯЗАТЬСЯ» напрямую использует введенную ссылку на Telegram из настроек.
  * **Синхронизация админки с сайтом**: проверена сквозная передача всех разделов (Клиенты, Услуги, Обо мне, Настройки, Рилсы, Проекты, FAQ) между `localStorage`, `/api/content` и главной страницей.

- **Блок F.A.Q.**: список вопросов теперь занимает **100% ширины** правой секции (ограничение `max-w-[553px]` убрано, вопросы и раскрывающиеся карточки растягиваются на всю доступную ширину).

- **Превью в Hero Area и Разделах работ в админке**:
  * Вместо статичной заглушки теперь автоматически подтягивается и отображается **первый кадр из самого видеофайла** (`<video src={`${video_url}#t=0.001`} preload="metadata" />`).
  * Если задана отдельная картинка-обложка, отображается она.
